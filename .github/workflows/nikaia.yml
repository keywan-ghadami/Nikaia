name: Nikaia

# **Once per change, not twice.** A push to a pull request's branch used to
# start the workflow as a `push` and again as a `pull_request`, the same
# commit twice. Pull requests run as `pull_request`; `main` runs on its push;
# everything runs weekly and by hand.
on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:

# A newer push to the same pull request makes the older run's answer moot.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always

jobs:
  # Which of the slower jobs this change needs (`scripts/ci-changes.sh` says
  # why for each). The compiler's own job always runs.
  changes:
    runs-on: ubuntu-latest
    outputs:
      unsafe: ${{ steps.decide.outputs.unsafe }}
      floor: ${{ steps.decide.outputs.floor }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Decide
        id: decide
        run: sh scripts/ci-changes.sh
        env:
          EVENT: ${{ github.event_name }}
          REF: ${{ github.ref }}
          BASE: ${{ github.event.pull_request.base.sha || github.event.before }}

  # The oldest Rust Nikaia claims (ADR-219), with the whole suite run on it:
  # the compiler, `std`, and every program the tests emit. The channel for
  # building stays `rust-toolchain.toml`'s (ADR-001 D1); this checks the claim.
  #
  # On every push to main and weekly; on a pull request only where a floor is
  # declared or can move (a manifest, the lock file, the toolchain).
  rust_floor:
    needs: changes
    if: needs.changes.outputs.floor == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Cache cargo registry and the floor's build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target/floor
            target/floor-unsafe
          key: ${{ runner.os }}-cargo-floor-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-floor-

      - name: Run the suite on the floor
        run: sh scripts/check-floor.sh

  # The crates that hold `unsafe`, one topic each (ADR-218). Each is its own
  # workspace and is checked on its own: tests and lints on stable, and Miri on
  # nightly - which checks the crates' tests and nothing the compiler builds,
  # so ADR-001 D1's one toolchain for building stands.
  #
  # Only where one of them, or the script that checks them, changed: nothing
  # outside a crate is in its build.
  unsafe_crates:
    needs: changes
    if: needs.changes.outputs.unsafe == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install stable, and nightly with Miri
        run: |
          rustup show
          rustup toolchain install nightly --component miri --profile minimal
          cargo +nightly miri setup

      - name: Check every crate under crates/unsafe
        run: sh scripts/check-unsafe-crates.sh

  # The compiler, on the toolchain a clone of this repository builds with.
  #
  # There is one leg because there is one toolchain (ADR-001 D1): nothing in the
  # workspace needs a nightly, so everything runs on `rust-toolchain.toml`'s
  # stable - the `rust` backend, the interpreter, projects, the ledger, the whole
  # test suite.
  bootstrap_compiler:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Version pinning per ADR-001 D1: `rust-toolchain.toml` is the single
      # source of truth for the channel and its components, and `rustup show`
      # installs exactly what it names, so nothing is duplicated here and
      # nothing can drift.
      - name: Install the toolchain the repository names
        run: rustup show

      - name: Cache cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-stable-${{ hashFiles('**/Cargo.lock', 'rust-toolchain.toml') }}
          restore-keys: ${{ runner.os }}-cargo-stable-

      - name: Verify Environment
        run: |
          rustc --version
          cargo --version
          echo "Nikaia Experiment: Environment Ready"

      - name: Check formatting
        run: cargo fmt --all --check

      # A decision is only citable if its number is stable, so a dangling
      # `ADR-023 D7` is as much a defect as a dangling link. 700+ citations
      # live in the compiler's own comments and tests.
      - name: Check ADR citations resolve
        run: python3 scripts/check-adr-refs.py

      # The roadmap's percentages are counted from its own boxes. That table is
      # the one thing on the page nothing else would catch going stale: every
      # box below it is a sentence somebody edits, and the number at the top is
      # a sentence nobody re-derives.
      - name: Check the roadmap's numbers
        run: python3 scripts/check-roadmap-numbers.py

      - name: Build the workspace
        run: cargo build --workspace --release

      - name: No clippy warnings
        run: cargo clippy --all-targets -- -D warnings

      # Every failure at once, rather than the first test binary's.
      - name: Test
        run: cargo test --workspace --release --no-fail-fast

      # The driver needs an input file, so run it on the samples rather than
      # bare `cargo run`, which only prints clap's usage error and exits 2.
      - name: Run Nikaia Driver (The Magic)
        run: |
          for sample in tests/samples/*.nika; do
            echo "::group::$sample"
            ./target/release/nikaia --input "$sample" --backend interpreter
            echo "::endgroup::"
          done
