[package]
name = "ref-or-box"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "Apache-2.0"
description = "One word that holds either a reference to static data or an owned box, with a niche for Option."
publish = false

[dependencies]

[workspace]
