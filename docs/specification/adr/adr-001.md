# ADR-001: The Rustc Driver Pattern — One Pinned Nightly, and a Generated Parser

**Status:** **Partly superseded** by [ADR-003](adr-003.md) — the coupling only.
§1.3 (version pinning), §2.3 (the parser backend) and §4.2 (the bootstrap
stages) still stand and are cited by live records; each is marked below.
**Date:** January 17, 2026
**Version:** 0.0.4 (Implementation Draft)
**Superseded in part by:** [ADR-003](adr-003.md) — §1.1, the `nikaiac::driver`
module in §2.1, and Chapter 3: the direct linkage to `rustc_driver`/`rustc_ast`,
replaced by Bridge-IR
**Still cited by:** [ADR-005](adr-005.md) §1 (the pinned nightly enables
`-Zpolonius=next`), [ADR-007](adr-007.md) (the parser backend),
[ADR-011](adr-011.md)/[ADR-012](adr-012.md)/[ADR-014](adr-014.md) (Stage 0 is a
transpiler), [ADR-021](adr-021.md) (`rust-toolchain.toml` as the single source
of truth)

> **What ADR-003 replaced.** This record describes the frontend linking the Rust
> compiler's internals directly. That is gone: the frontend emits **Bridge-IR**
> and an orchestrator owns the `rustc` side ([ADR-003](adr-003.md)). Read §1.1,
> the `nikaiac::driver` entry in §2.1, and Chapter 3 below as history.
>
> Everything else was never about that coupling and survived it — *which*
> toolchain, *which* parser backend, and *what the bootstrap stages are*. Those
> sections are in force and are cited by live records; see the index's
> [supersession section](README.md#what-supersedes-what).

---

## Chapter 1: Introduction and Context

> **History.** §1.1 below is the decision [ADR-003](adr-003.md) reversed.

This document defines the fundamental technical implementation of the Nikaia compiler (`nikaiac`). It documents the architectural decision to move away from a classic transpiler approach (Source-to-Source) in favor of a monolithic integration into the Rust Compiler Frontend (**"Rustc Driver Pattern"**).

### 1.1. The Driver Approach
Nikaia is not implemented as an external preprocessor that writes `.rs` files to disk. Instead, `nikaiac` acts as a wrapper around `rustc`, leveraging its internal APIs (`rustc_driver`, `rustc_interface`) directly.

**The Compilation Flow:**
1.  **Frontend (Nikaia):** Parsing `.nika` files into the Nikaia AST.
2.  **Semantic Analysis:** Validating Profile Constraints (Lite vs. Advanced) and resolving Extensions.
3.  **Lowering (In-Memory):** Transforming the Nikaia AST directly into a Rust `TokenStream`.
4.  **Injection:** Passing the `TokenStream` to the Rust compiler process as virtual input.
5.  **Backend (Rustc):** Macro expansion, Type Checking, Optimization (LLVM), and Code Generation.

### 1.2. Motivation
* **Macro Support:** Generated code must enter the compiler *before* macro expansion occurs.
* **Configuration Injection:** The driver dynamically sets flags based on the profile (e.g., `-C panic=abort` for Lite).
* **Error Mapping:** Using internal `Span` structures allows mapping errors directly back to the `.nika` source file, rather than the generated Rust code.

### 1.3. Version Pinning

> **In force.** Not touched by [ADR-003](adr-003.md), and load-bearing:
> [ADR-005](adr-005.md) may enable `-Zpolonius=next` only because of it, and
> [ADR-021](adr-021.md) hashes the pinned toolchain into the build-cache key.

Since Rust's internal APIs are unstable, Nikaia enforces strict coupling to a specific compiler version. Every Nikaia release includes a `rust-toolchain.toml` file pinning an exact Nightly build of Rust.

---

## Chapter 2: The Compiler Implementation (`nikaiac`)

The compiler is written in Nikaia itself ("Dogfooding").

### 2.1. Module Structure
* `nikaiac::ast`: The Data Model (Abstract Syntax Tree).
* `nikaiac::parser`: The Grammar definitions.
* `nikaiac::analysis`: Semantic analysis and method resolution.
* `nikaiac::lowering`: Transformation Engine (AST -> TokenStream).
* `nikaiac::driver`: Interface to `rustc_driver`. *(Superseded: the frontend
  speaks Bridge-IR and links no compiler internals — [ADR-003](adr-003.md).)*

### 2.2. The Data Model (`nikaiac::ast`)
The AST reflects the **Unified Types** and new **Arrow Syntax**.

```nika
// ast.nika
pub enum Expr {
    LiteralInt(i64),
    LiteralStr(String),
    Variable(String),
    
    // Binary Operation: 1 + 2
    Binary { 
        op: BinOp, 
        left: Shared[Expr], 
        right: Shared[Expr] 
    },
    
    // Block: { ... }
    Block(Vec[Stmt]),
    
    // Closures (Arrow Syntax)
    // Covers: arg => expr, (a,b) => { ... }, and { ... } (Block Lambdas)
    Closure {
        args: Vec[Ident],
        body: Shared[Expr],
        is_move: bool // 'move' keyword
    },
    
    // Async Control Flow
    Select(Vec[SelectBranch]),
    Spawn(Shared[Expr])
}
```

### 2.3. The Parser (`nikaiac::parser`)

> **In force.** The choice of parser backend is independent of how the frontend
> reaches `rustc`, so [ADR-003](adr-003.md) left it alone.
> [ADR-007](adr-007.md) builds the grammar protocol on it.

The parser is generated by the **`winnow-grammar`** crate, maintained as a separate
repository at [`keywan-ghadami/winnow-grammar`](https://github.com/keywan-ghadami/winnow-grammar)
and consumed as a pinned git dependency. It compiles an EBNF-like grammar, written inline
in Rust via the `grammar!` macro, into [`winnow`](https://docs.rs/winnow) parser combinators
at compile time (zero runtime overhead).

**Goal:**
The strategic objective is unchanged: express the Nikaia parser as a declarative grammar and
eventually self-host it — a Nikaia parser written in Nikaia via the native `grammar` construct
(Part II, Chapter 10).

**Why not `syn-grammar`:**
An earlier revision of this ADR specified `syn-grammar`. That was the wrong tool.
`syn-grammar` parses a Rust **`TokenStream`**, so it inherits Rust's lexer:

*   Nikaia syntax that is not a valid Rust token sequence cannot be expressed at all.
*   Whitespace and token boundaries are decided by Rust before the grammar ever runs, which
    makes the "host a foreign DSL" goal (Part II, 10.5) impossible: an embedded SQL or
    assembly block would have to survive Rust tokenization first.
*   Trivia (comments, layout) is discarded before the grammar sees it, which rules out
    tooling use cases such as a formatter or an LSP.

`winnow-grammar` instead parses `&str` / `&[u8]` directly. It is built on the same
`syn-grammar-model`, so the grammar syntax carries over, but the input is raw text.
This makes the toolchain **scannerless** — see [ADR-007](adr-007.md), which promotes that
property from an implementation detail to a language guarantee.

**`winnow-grammar` features this architecture relies on:**

| Feature | Use in Nikaia |
| :--- | :--- |
| EBNF syntax (`\|`, `?`, `*`, `+`, groups) | Rule definitions |
| Type-safe actions (`-> { … }`) | Direct AST construction, no untyped tree walk |
| Lexical vs. syntactic rules (UPPERCASE / lowercase) | Whitespace control without a separate lexer |
| Cut operator (`=>`) | Commit points — error messages instead of silent backtracking |
| `recover(rule, sync)` | Fault-tolerant parsing for LSP/tooling (Part II, 10.7) |
| Automatic direct left recursion | Natural expression grammars |
| Span binding (`@`) via `LocatingSlice` | Source positions for diagnostics |
| Rule arguments and generic rules | Reusable patterns (e.g. `list<T>(item)`) |
| Own diagnostics engine (`ParseError` with rule stack) | Backs the diagnostics contract (Part III, Appendix C) |
| String interner (`Symbol`) | Identifiers in the AST; cheap equality, one copy per spelling (Part II, 10.6) |
| `fold(rule, init, step)` | An entry rule that reduces instead of collecting, so a large input parses in constant space |

**Not provided by the backend today** (tracked in ADR-007 as deferred): auto-generated AST
types, and regex terminals — `winnow-grammar` offers built-in terminals (`ident`, `string`,
`float`, `hex_digit1`, …) instead.

```rust
// crates/nikaia/src/parser/mod.rs
use winnow_grammar::grammar;

grammar! {
    grammar CompilerGrammar {
        use crate::ast::*;

        // `rule program` generates `CompilerGrammar::parse_program`.
        pub rule program -> Program =
            items:item* -> { Program { items } }

        // Cut operator: once "let" matched, we commit to this alternative
        // and report an error instead of trying the next one.
        rule let_stmt -> Stmt =
            "let" => mutable:"mut"? name:ident "=" val:expr -> {
                Stmt::Let { name, mutable: mutable.is_some(), value: val }
            }
    }
}
```

**Version pinning:** `winnow-grammar` is pinned by git revision in `Cargo.lock`, not by
semver. The pin and the workspace `winnow` version must be advanced together — the crate
tracks `winnow` closely, and its diagnostics engine replaced `winnow::error::ContextError`
with its own `ParseError`, which is a breaking change for code that matches on error text.

### 2.4. Semantic Analysis (`nikaiac::analysis`)
This phase enforces constraints specific to **Lite** or **Advanced** profiles.

```nika
// analysis.nika
pub fn check_constraints(module: &Module, profile: Profile) throws CompileError {
    // Constraint Check
    if profile == Profile::Lite {
        for stmt in module.stmts {
            if let Stmt::Import { path } = stmt {
                // Forbid manual threading in Lite
                if path.starts_with("std::thread") {
                    throw CompileError(
                        "Module 'std::thread' is forbidden in Lite Profile. " +
                        "Use 'spawn' for safe concurrency."
                    )
                }
            }
        }
    }
    register_extensions(module)
}
```

---

## Chapter 3: The Lowering Engine (`nikaiac::lowering`)

> **History.** Lowering no longer produces Rust tokens for a hijacked
> `rustc_driver`. The frontend emits **Bridge-IR** and `rustc-executor` takes it
> from there ([ADR-003](adr-003.md), [ADR-004](adr-004.md)); the Nikaia-side
> lowering as actually built is [ADR-011](adr-011.md). What follows is kept
> because it records the shape the decision was made against.

The core of the compiler is the `LoweringContext`, which translates Nikaia constructs into Rust tokens.

### 3.1. Transformation Logic
This section demonstrates how the **Advanced Profile** and **Block Lambdas** are handled.

**Example: Lowering a `spawn` call**

*Input (Nikaia AST):*
`spawn({ do_work() })` (Block Lambda)

*Lowering Logic (Pseudocode in Compiler):*
```rust
fn lower_spawn(closure_body: NikaiaExpr) -> TokenStream {
    // 1. Profile Check: Lite or Advanced?
    // We select the appropriate runtime backend.
    let spawner_func = match self.profile {
        Profile::Lite => "tokio::task::spawn_local",
        Profile::Advanced => "tokio::task::spawn" // formerly 'Standard'
    };

    // 2. Token Generation
    // We inject `async move` because Nikaia tasks are implicitly async state machines.
    // The user writes synchronous-looking code inside the block,
    // but we wrap it in an async block for the Rust compiler.
    quote! {
        #spawner_func(async move {
            #closure_body // Recursive lowering of the body
        })
    }
}
```

*Output (Rust TokenStream):*
`tokio::task::spawn(async move { do_work().await })`

### 3.2. Lowering Extension Methods
To map Ad-hoc Extensions (Chapter 4.2) to Rust, the compiler uses "Synthetic Traits".

```rust
// Generated Rust TokenStream
trait __NikaiaExtension_String_GenId123 {
    fn is_email(&self) -> bool;
}

impl __NikaiaExtension_String_GenId123 for String {
    fn is_email(&self) -> bool { ... }
}
```

### 3.3. Error Mapping (Span Preservation)
Nikaia constructs Rust tokens using Spans that point to the original `.nika` source file. This ensures that compiler errors highlight the Nikaia code, not the generated Rust boilerplate.

---

## Chapter 4: Verification and Bootstrapping

### 4.1. Codegen Tests
We use Snapshot Tests to verify that the lowering logic respects the profile.

```nika
// tests/codegen_test.nika
test "Spawn Lite Profile Generation" {
    // Input: Block Lambda Syntax
    let snippet = "spawn { 1 + 1 }"
    
    // Action
    let output = nikaiac::compile_snippet(snippet, Profile::Lite)
    
    // Assertion
    // Must use local spawner (Single Threaded)
    assert output.contains("tokio::task::spawn_local")
    // Must NOT use threaded spawner
    assert !output.contains("tokio::task::spawn(") 
}
```

### 4.2. Bootstrap Stages

> **In force.** "Stage 0 is a transpiler" is cited as the premise by
> [ADR-011](adr-011.md), [ADR-012](adr-012.md) and [ADR-014](adr-014.md).

As defined in Part III, Chapter 17, the build occurs in stages.
1.  **Stage 0:** Rust Prototype.
2.  **Stage 1:** Self-Hosting (Nikaia compiling Nikaia).
3.  **Stage 2:** Verification (Stage 1 compiling itself).

The result of Stage 2 must be capable of processing the Nikaia Standard Library and user code without errors.
