# Architecture Decision Records

A record here answers one question: **what was decided, and what made that the
answer.** It is written once, it is not edited as the code moves on, and when a
later decision changes it the change is recorded as a new ADR that says which
one it displaces.

A decision the project **withdraws** is the exception, and it is removed rather
than superseded: the records are rewritten as though it had never been there,
with one sentence for what is done instead. A reader is owed the reason for the
current answer, not the route to it — the route is `CHANGELOG.md` and the notes.
The test of which case you are in: a supersession leaves a live citation behind,
a withdrawal leaves none ([`docs/README.md`](../../README.md) §2).

## What goes where

| | holds | does not hold |
| :--- | :--- | :--- |
| the [specification](..) | what the language *is*: the rule, the syntax, the guarantee | why that rule won, what the alternatives were, what anything cost |
| **an ADR** (here) | one decision, the reasoning that settled it, and the evidence it rests on | the story of the session that produced it |
| the [notes](../..) and `CHANGELOG.md` | what was tried and measured, how, and what went wrong on the way | anything normative |

A measurement belongs in an ADR as *the number that decided it*, with the method
in the notes. A spec section states the rule and links here; a reader who only
wants to write Nikaia never has to open one. The full rule is
[`docs/README.md`](../../README.md).

## The index

**Status** is one of *Accepted*, *Open* (decided nothing yet), or *Superseded*.
**Built** says what of it exists in the compiler today — an ADR is a decision,
and a decision is not an implementation.

### Compiler and toolchain architecture

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [200](adr-200.md) | **`nikaia serve` is cut, and the MVP is the application server alone.** [194](adr-194.md) answered *who decides what is exposed* by finding the question false — there are **two products**, and each is right about itself — and that reasoning is not withdrawn. What changed is the project's position: the MVP is **behind schedule**, and one of the two products is worth much less than the other. D1 cuts the standard file server for two reasons stated together. **Schedule**: `serve` is the half of D2 that nothing else waits on — [018](adr-018.md) entire, [058](adr-058.md)'s body rows and the roadmap's route hashing all wait on the *application* server, and not one of them waits on a file server. **Value**: `python -m http.server` is what it would have imitated, and what a Nikaia one would demonstrate about this language is the part it has least of — no handler, no routing, no ledger, no provenance. D2 is the subtraction that follows: [194](adr-194.md) D5 ends *the limits are the operator's in the first product and the program's in the second*, so with the first gone the second clause is the whole sentence, and D3's localhost default goes the same way — an application's author is present and says which address. What D3 leaves behind is `--trust`'s listing of a wide bind, which it put in [108](adr-108.md)'s shape rather than its own. D3 is what replaces the command: `examples/hello-http/` is a real server, *one address and one function*, driven by a test over a real socket. **A cut is written down**, which is the whole of why this record exists — a step nobody intends to take is worse than an absent one, because it makes the list wrong about what is coming rather than merely incomplete | Accepted | **yes** — the command is not in the CLI and is not going to be; the server it was the other half of is in `examples/http/` and answers on a socket |
| [199](adr-199.md) | **A readiness wait is a registration, not a worker.** It used to be a `worker::Op`: the descriptor was duplicated, handed to an I/O worker, and the worker **blocked in the poller for the whole of the wait** — with `io-workers` at **1** by default ([038](adr-038.md) D4), so a wait that had not answered blocked every other wait in the process. Measured before anything was written: two waits at once, the second on a pipe that already had a byte in it, and the second did not get a turn in two seconds. **A ceiling on the default configuration and not a slowness**, and the shape an HTTP server has exactly. D1 is one poller for the process with one thread inside its `wait`, and arming on the **calling** thread, because registering is an `epoll_ctl` and does not block. **D2 is [009](adr-009.md) D4 doing its job**: `open-work.md` §2.6 called this step *keep registrations*, and the numbers say keeping them is worth 6.2 µs of 35 while the hop was worth 28 — the line was written before anything had been measured. `rt::io::waiting`, which is what a server uses, goes 35.4 → **6.6 µs**. D3 keeps the promises the worker path made: a registration is *outstanding*, so `pending` counts it and [006](adr-006.md) D5's drain waits it out. D4 keeps the bell, because a `Waker` does not reach a thread asleep in `io_uring_enter`. D5: dropping the future deletes the registration | Accepted | **yes** — the module, both surfaces, `Op::Readiness` and `poll_one` deleted, and the two-second stall written down as a test so it cannot come back |
| [198](adr-198.md) | **`std::net` — the socket `std` lends, and the first untrusted source.** [194](adr-194.md) D1 decided *where* a socket lives and said nothing about what it looks like; Part III 17.1 had one line and nothing behind it, and three records wait on something for a handler to run **for**. D1 is four verbs and two types — `listen`, `connect`, `accept`, `read`, `write`, and `address`, `peer`, `close` — with `read` handing back [156](adr-156.md) D1's one shared buffer, `write` taking **all** of it because the loop that would follow a partial write is inside this, and an empty read meaning the peer closed. **D2 is the one that had been waiting**: what comes off a socket is `untrusted`, and it is the first source in `std` that is — `std.contracts`' own header predicted it by name, and the analysis that reads it was built long before anything wrote it. D3 gives `touches` a `socket` kind, naming no socket for `lock`'s reason: a connection is what a program touches and telling two apart is the alias analysis [039](adr-039.md) D4 refuses. D4: everything that waits gives the thread up on [121](adr-121.md) D4's readiness, so one thread serves many connections at `user_parallelism = no` exactly as at `yes` — and nothing in a `.nika` file that binds a socket says `async`, `await`, `epoll` or `poll`, which is [038](adr-038.md) D3 held where it is hardest to hold. `bind` and `connect` block, stated rather than hidden. D5: closing is the drop, and [006](adr-006.md)'s `Cleanup` is not waited for | Accepted | **yes** — the module, nine ledger entries, the kind, and two test binaries; the `.nika` one runs a program that binds, accepts, reads and writes at both settings and compares what it printed |
| [197](adr-197.md) | **A `path` is relative to the file it is written in.** `nikaia.toml` named a Rust crate `path = "../../../../shim"` — four `..`, because [002](adr-002.md) D1 said the value reaches Cargo **verbatim** and the generated manifest used to sit at `target/nikaia/build/Cargo.toml`. [053](adr-053.md) D1 gave every package a member directory of its own, so the manifest Cargo resolves against is one level deeper and every one of those `..` is one short: all three projects under `examples/foreign-runtime/` have been unbuildable since, and nothing said so — the tests that build them fetch `hyper` from crates.io and are `#[ignore]`d, and CI does not run ignored tests. D1 makes a `path` relative to `nikaia.toml` for **both** arms of `[dependencies]`, which is what a reader would guess and what a Nikaia package's `path` already did ([047](adr-047.md) D2). D2 narrows D1's passthrough by exactly one key: a relative `path` is resolved and written **absolute**, which the generated manifest already does for `nikaia-std`, and everything else still travels untouched. The alternative was counted out and refused — `../../../../../shim` is a number a person derives from a build layout that changed once already without anybody noticing. D3: what a `path` is relative to is a property of the **file** and not of the ecosystem, so the two arms stop needing two rules | Accepted | **yes** — the three manifests, `describe::crate_sources` and `crate_root`, and `project::cargo_member`. And the disagreement it closes was the compiler's own: `nikaia describe` and `cargo build` looked in two different places for one crate's sources |
| [196](adr-196.md) | **A Nikaia piece joins a Rust program as an ordinary Cargo module.** [195](adr-195.md) decided `describe` is written in Nikaia and not how the result reaches the Rust program that calls it. Of the three routes — a **process**, the **C ABI**, the **same compilation** — the owner named the third and the tree already runs it: `crates/nikaia-std/src/text.nika` is lowered ahead of time, committed and `include!`d, and a Rust caller reaches it with a plain call and plain types. D1 makes that the route: no wire format, no serialisation, no `unsafe`, no second binary, and **no interface to design**, because a boundary whose types are Rust's own is one nothing has to agree about. D2 extends it past the instance it was found on — `describe`'s parser and [038](adr-038.md) D6's HTTP/1.1 parser, which is [194](adr-194.md) D5's *Rust until it is not* — and keeps the owner's second point: the lowered form is an ordinary Cargo module, so a thing written in Nikaia is available to Rust programs that have never heard of it. D3: the compiler's `nikaia-std` dev-dependency becomes a real one, and there is no cycle, because `nikaia-std` may not depend on the compiler ([002](adr-002.md) D4). D4 narrows [195](adr-195.md) D4 — the Rust half keeps the I/O and hands the Nikaia half the text, so the walk and the subprocess leave the critical path — and **names the risk that buys**: a route that makes a partial move cheap makes a permanent one cheap, so the split is a staging with a written end and a stated sign of having stalled | Accepted | **partly** — the route since [014](adr-014.md). The grammar is `crates/nikaia-std/src/tools/rust.nika`, lowered by `nikaia lower-std`, `include!`d as `nikaia_std::tools::rust`, and driven from Rust by `crates/nikaia-std/tests/rust_signatures.rs`: it reports no item that is not one where the scanner reports four on the same text. D3's edge is drawn - `nikaia-std` is a real dependency of the compiler, four packages it did not already resolve - and the scanner is **out** of `describe.rs`, `translate` untouched. That record's third limit is **closed**: a module is a block or a file, `src/foo/bar.rs` is `foo::bar`, and whether a caller may write a path is read from the `pub mod foo;` in its parent - a module nothing declares is not offered, which is [010](adr-010.md) D1's polarity. Of the scraper's three named limits only the **macro** one stands, and it is [001](adr-001.md) D1's rather than any parser's. §4's *where the `.nika` lives* is answered — under `src/tools`, where the release step now also looks: a `.nika` beside `lib.rs` is a module **of** `std` and `std.contracts` has to carry it, so what the toolchain uses and `std` does not publish gets a directory of its own |
| [001](adr-001.md) | Stable is the toolchain, named in one file; the parser is generated from a grammar over bytes, not over Rust tokens; the build is staged and Stage 0 is a transpiler | Accepted | yes — one toolchain, one CI leg |
| [002](adr-002.md) | The CLI wraps Cargo so crates.io works; compile-time code runs in an interpreter, not as a proc-macro; a project reaches `std` through a sysroot of pre-lowered sources, compiled into a keyed rlib cache | Accepted | D1 for a Rust dependency; D4 - sysroot, pre-lowered `std`, rlib cache (97 packages to 46, 58 of them the compiler built twice); not a Nikaia dependency, not D2 |
| [003](adr-003.md) | The language and the machinery that compiles it are two programs, and the interface between them is Rust source text; CLI, cache and Cargo wrapping are generic | Accepted | yes |
| [004](adr-004.md) | One lowering, and it emits Rust source text; the text a person reads is the text that is compiled | Accepted | yes — `crates/nikaia/src/emit`, what a bare `nikaia` runs |
| [021](adr-021.md) | The build cache is ours; what is hashed into its key, and what invalidates what; a backend that is not here refuses by name (D9) | Accepted | key, store, lockfile - D2's resolved versions included (§5); D9's refusal; not `--locked` for the lock |
| [103](adr-103.md) | **A package is found by version through Cargo, under the crate name `nikaia_<name>`.** `http = "1.2"` resolves on crates.io as `nikaia_http` through Cargo's own rename, so the prefix appears in one generated line and never in a `.nika` file; a `git` table with a `tag` is the same arm without an index; the path arm stays. A published crate holds `nikaia.toml`, the sources and the ledger, and a stub `Cargo.toml` for the resolver only — nothing lowered, because the lowering is the consumer's. A crate under the prefix without sources and ledger is refused by name. Cargo's semver, Cargo's lockfile column, Cargo's fetch; what the project gives up is an index of its own, reversibly | Accepted | **no** — a bare version is refused with the old sentence |
| [104](adr-104.md) | **A foreign crate is described before it is called.** A call into a crate no ledger describes is refused, naming `nikaia describe <crate>`, which writes a draft ledger for the functions the program calls — from rustdoc-JSON where the toolchain offers it, from the sources where it does not — translated by 15.2's table, `touches` and `locks` fail-closed, committed as `contracts/<crate>.contracts`, hashed against the crate's version, and reviewed like code, which is [020](adr-020.md) D5's rule for `std`'s Rust half applied to every crate. Every analysis then reads an entry at the boundary, and C.1 becomes keepable at the one place it was not | Accepted | **D1 and D2** — the refusal, the entries it asks for (a description types the call, says whether it can fail and whether it pauses, and a described crate is a package rather than one of `std`'s modules), and the command that writes the draft: `nikaia describe <crate>` scrapes a `path` dependency's `pub` signatures and translates them by 15.2's table, asserted against the reviewed file the experiment already carried, entry for entry. For two records the description was parsed to see whether it parsed and the ledger dropped, so its only effect was silencing the refusal that had asked for it. **D5's hash rule**: a description is believed while the files it was derived from hash as recorded, and `NK2505` says so where one does not — a ledger is derived again there and a description is *reviewed* again, because what it says is a person's judgement. The rustdoc-JSON reader and a version dependency's sources are §5's remaining steps |

### Ownership, borrowing, cleanup

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [005](adr-005.md) | The borrow model: four groups of lifetime situation, and which the compiler solves silently. No lifetime annotations, ever. Group B.2 is the frontend's own desugaring (D2) | Accepted | inference, ledger, D8's cross-process check (not its second OS), and §1 Group B's structural `Send` check — `NK2501`/`NK2502`, one verdict at both settings of `user_parallelism` (§5). D7 now translates a trait bound's **position**; its text stays Rust's. D2's desugaring: not built |
| [006](adr-006.md) | `Cleanup` — teardown that performs I/O, inserted by the compiler on every exit path | Accepted | no |
| [008](adr-008.md) | Tethered slices in user structs: the tether-state lattice, and one handle per container rather than per token | Accepted | views, and **D9** — a result that borrows from nothing says `'static`, which is the only lifetime that can be written there; the body is still checked against it. `@borrowed` and the retention lint are not built |
| [064](adr-064.md) | **`SharedMut[T]` names the capability**, not the mechanism, and survives to the emitter - one name above, two hulls below, chosen per value like the count. **A hull you can observe, you write**: `Shared(x)`, `SharedMut(x)` and `Locked(x)` are calls, so the annotation stops being the constructor and the two positions that could make one stop being a list. And `Shared[Locked[T]]` is **refused** - one type, one spelling | Accepted | **yes** — and Part II 12.2's counter compiles and runs at both settings for the first time, which is the program `user_parallelism` exists for. Four holes closed by one change, because they were one hole: a number literal could not be given a hull at all while a *position* had to know one was wanted |
| [065](adr-065.md) | **A door over several locks is a call the compiler types** — not a ledger entry (a signature binds from a receiver and there is none, the locks hold different types) and not a construct (the grammar has taken the shape since the trailing lambda). **`update_all` takes one `mut` per lock**, which is `update`'s rule widened rather than a second rule — nothing returned, nothing seen between the two writes, and copy-or-address by the pair of types ([110](adr-110.md) D6). The order is by **address**, so two tasks naming the same two locks the other way round take them alike | Accepted | **yes** — Part II 12.3's transfer runs at both settings, and two threads transferring in opposite directions neither deadlock nor lose anything. Two locks at a time; `NK1124` for a door handed what is not one. The ledger carries the `sync` column with **no signature**, which is the half it can say |
| [067](adr-067.md) | **`sync` promises one thing — it never pauses** — and Part II's prose, which also promised no I/O, is corrected: `println` is `sync` and so is a panic hook, which **may block**. What a function *reaches* is the second question, and **`touches` gets the inference it was written for** — the fourth derived column, specified with the same fail-closed polarity as the three beside it and never filled. **`lock` joins the vocabulary** the day the doors exist | Accepted | **yes** — `touch::infer` over the call graph, reusing the one call walk the checker and `sync` share; `lock` on all ten doors. One missing pass, three consumers: the lock-inside-a-lock refusal has its fact, `overlap` sees past a call into user code, and *"touches nothing"* is very nearly *"repeating it is unobservable"*. No new word — neither `pure` nor `repeatable` |
| [039](adr-039.md) | The lock: two representations stay and the difference becomes unobservable — taking a lock inside a lock is refused, a second derived property says whether a function touches one, the shared mutable type is `SharedMut[T]`, and it has four doors instead of one | Accepted | **D9 and D10** — `Locked` since [057](adr-057.md) and [059](adr-059.md), `SharedMut` since [064](adr-064.md), both with their four doors and their `std` entries. **D6 since the walk it generalises learned to say why it refused** — `NK2503` names the call, the argument and the field, and prints the way out as a line the program can be edited into. What is left is D2's refusal of a lock inside a lock, D3's lock-touching property, D7's lambda rule and the `NK22xx` codes that are catalogued and not emitted |
| [045](adr-045.md) | The crossing verdict takes the **destination**: into a task of our own a lock may cross at both settings, into undescribed foreign code it may not — conservative at `yes` and deliberately so | Accepted | **yes** — `send::crossing` takes a destination and the lock is the row it reaches: `NK2502` refuses a lock handed to an undescribed call, a task and the overlapping closure take one. The type is built too since [064](adr-064.md), so Part II 12.2's counter is not only writable but **runs**, at both settings |
| [094](adr-094.md) | **The caller writes no `&`.** Whether a parameter is lent or kept is the callee's — a view unless the body keeps it, inferred over the call graph into a `keeps` column beside `returns`, with `&T` in a declaration kept as the assertion the way `sync = true` is — and the compiler writes the reference at the call, as it writes the `.await` and the `?`. A `for` lends and a `let` over a place is a view; mutation of a parameter is `mut` in the declaration, never at the call, which is `&mut self`'s rule for every parameter. The one semantic cost — a kept value's cleanup point moves into the callee with nothing at the call — is accepted the way `close(self)` already is and narrated by the ledger diff. 42 `&` at calls in 913 lines of `examples/`, every one repeating what the callee's signature had said | Accepted | **steps 1–4** — the `keeps` column is inferred over the call graph and recorded beside `returns` (six of 39 functions in `examples/` keep a parameter; six of `std`'s 94 entries do), a `for` over a place **lends** it, a `let` over a place is a view where the value would move, `xs.drain()` takes the elements away, and a parameter the callee only reads is declared `&T` and given its `&` at every call off that one column, with `NK1137` refusing a written one in either position. Not lent: a kept parameter, a copy type, an argument already a view, a method's argument. `mut out: Vec[i64]` is the third state and lowers to `&mut T`, and `NK1138` refuses a parameter a body changes without the word. Only D5's narration is left in §5 |
| [107](adr-107.md) | **Text is one type, `String`, whose state the compiler picks — and `&str` is the assertion that it is a view.** A literal stored where a `String` is declared is a view of static text and allocates nothing; whether a text value is borrowed, tethered or owned is 6.6's rule applied to text, as `Bytes` already has it. `&str` stays as the promise *no copy, no handle*, held to at the line that would break it — [094](adr-094.md) D2's `&T` and `@borrowed` with the spelling the language had. A copy is `.to_owned()` or a refusal, never inserted; `--tethers` says what was chosen. Costs one word per text value and buys the door for the hot loop | Accepted | **no** — two types in the checker, and `NK1106` says *write `.to_string()`* |
| [108](adr-108.md) | **A path names its root at the call.** Every `std` function that takes a path takes its root right after it, with no default: an `fs::Root`, which is `Dir(store)` — the name is resolved under it and compared component by component, `fs::Outside` where it would leave — or `Anywhere`, the one way around the check, recorded per site and listed by `nikaia --trust`. No exception for a literal, whose meaning is the working directory's. No provenance travels a path and no analysis follows one: the taint Perl had is the door this keeps shut, because only `std`'s own comparison clears anything. [010](adr-010.md)'s lattice keeps its one consumer, the hasher | Accepted | **no** — `fs::map(path)` takes one argument, and `fs::Root` does not exist |
| [109](adr-109.md) | **A trait method may pause unless it says `sync`, and the language below is Rust 1.75 or newer.** A declaration reads like a function type, [102](adr-102.md) D2's rule for a signature without a body: without `sync` it may pause, without `throws` it cannot fail, and an implementation is compared against that — `NK1129` becomes the comparison and `NK1140` is its `throws` twin. The emitter writes the return-position form in the trait, `Send` where `user_parallelism = yes`, `async fn` in the `impl`. Every generated `Cargo.toml` says `rust-version = "1.75"` from one constant, checked against `rustc --version` before `cargo` runs; the channel stays `rust-toolchain.toml`'s | Accepted | **no** — every trait method is `sync`, and no generated manifest names a version |
| [110](adr-110.md) | **An `update` block says `mut` and nothing else, may run more than once, and the compiler picks the lock.** `kasse.update fn(mut v) { v += 100 }` is the one form — [094](adr-094.md) D3's word, the lock as the caller — and a returned value is `NK1141`. What `v` is, is the compiler's by type: a **copy** where the value fits a machine word, run on the copy and compare-and-swapped in, run again on a collision; the **address** otherwise, run once. Nothing leaves the lock and no slot is empty. The retry [039](adr-039.md) §3 left open is permitted, because `sync`, `touches` and capture-by-view already make a second run invisible. `update_all` takes one `mut` per lock; `access` reads; `--sharing` names a large `get` | Accepted | **no** — `update` takes `T` and returns `T` in an `Option`, and no compare-and-swap path exists |
| [111](adr-111.md) | **What leaves a lock is stamped `Seen[T]`, and nothing stamped goes back in blind.** `get` and `access` hand out a `Seen`; the stamp sticks through arithmetic, through calls to anything whose `touches` names no lock, through declared fields and time, and never comes off. A `set` given a stamped value or under a stamped condition is `NK2205` wherever the read was; an `update` block that does not read its `v` is `NK2207`. `set(neu; after: seen)` is the one door for a stamped value, defined as the `update` that compares and throws `Overtaken`. The emitter erases the type: nothing at run time. The lost update, in two lines or two requests, cannot be written | Accepted | **no** — `get` returns `T`, and `NK2205` sees the inline `get` only |
| [112](adr-112.md) | **A cleanup that did not finish is a failure of the program.** An expired `cleanup-deadline` ends the program with exit status 70 (`EX_SOFTWARE`), and the message naming every resource cut off goes the panic path — stderr and the panic hook — never stdout. The specification had said *exits with a warning* and no status, and an unsaid status is `0`, which is what a supervisor reads. No setting makes it `0` again; `"0"` does not drain and never expires | Accepted | **no** — the drain it ends does not exist ([006](adr-006.md) D3) |
| [113](adr-113.md) | **`?.` reaches through a view, and the receiver stays usable.** `user?.name` then `println(user)` compiles: the reach is `as_ref()` below, and the result is a copy where the member copies and a view of the receiver otherwise, kept alive as a field read's is. The move Part I 3.5 called a rule was the lowering's, and [052](adr-052.md) D8's translation of it goes with it | Accepted | **no** — the lowering still takes the receiver |
| [114](adr-114.md) | **Reading a map through the brackets is a `T?`, because a key is data.** `scores[name]` answers what `get` answers; `m[k] = v` still inserts; `m[k] += 1` is written `m[k] = (m[k] ?? 0) + 1`; a list's `xs[i]` keeps its abort, since an index is the program's own arithmetic. One `index::get` with an output type per container, so the emitter writes one call as it does for the write | Accepted | **no** — a map read lowers to Rust's `Index` and panics on an absent key |
| [115](adr-115.md) | **An `overlap` keeps every failure.** The first failing branch in written order wins, as before, and the others are attached to it as its `secondary` list in written order — the list every error carries, which a cleanup error while unwinding already joined without a program being able to read it. The block waits for every branch, so the list is a fact and not a race; a branch handles its own failure and cannot see another's; combining is `catch` on the block. `catch` chooses by one error's type, unchanged | Accepted | **no** — the join drops every failure but the first |
| [116](adr-116.md) | **`from` is a name, and a file a build reads is `asset("…")`.** The word bought one phrase for a construct that is not built, and cost the pair every API reaches for — `fs::rename(from:, to:)` could not be written in the language. The build-time read becomes a call the compiler recognises in a `comptime` initialiser, with every rule of [072](adr-072.md) unchanged under it. Rust does not decide the list: what it reserves is escaped ([076](adr-076.md)) | Accepted | **yes** — `from` is out of the parser's table and out of the grammar's `RESERVED`, so it is a name in every position that declares one and Part III 17.1's `rename(from:, to:)` parses; the removed `dsl X from e` keeps its sentence from the **token** rather than from the reservation, which is D1's *loses nothing* read literally. It also caught a drift: `select` had reached the table and not Part I 2.1's printed list. D2's `asset("…")` is built with [072](adr-072.md) under it, and `NK1177` refuses one written outside a `comptime` with the name of the run-time read |
| [117](adr-117.md) | **`loop`, `const`, `macro` and `quote` are names, and their messages move to the undeclared-name help.** Each was reserved so that a reader from another language could be told something; a name nothing declares is told the same thing by `NK1117`'s help, as `assert` already is (`unsafe` was the other example and has since joined the list *with* a construct, [119](adr-119.md)). `with` stays, for the copy-with-changes construct that is its own record. The list goes from 37 to 33; Rust does not decide it, since what Rust reserves is escaped | Accepted | **yes** — 40 words to 36 (the list has grown since this record was written), the three sentences on `NK1117`, each ending by saying the word is an ordinary name otherwise. `const` and `loop` joined the emitter's escape list, which is D1's *answered as it is for `type`*: a Nikaia name can be one of them now, and `let mut loop = 1` lowers to `let mut r#loop = 1`, compiles and runs |
| [118](adr-118.md) | **`with` is a copy of a value with named fields changed.** `p with { x: p.x + 1 }`: the braces, the field list, the shorthand and the privacy rule are the struct literal's; only the top level; the unnamed fields are moved, never copied unseen, and a value used again afterwards is refused with the copy to write. Below it is a struct expression with a base. An enum operand is left open and refused for now | Accepted | **yes** — one postfix rule, the fields resolved against the operand's type, and Rust's functional update below. `NK1173` is the operand, with a reason per shape: an enum, a **view** (there is nothing to move out of one), a type with no fields, and a value whose type this compiler did not work out — the lowering writes the type's name, so it is the checker's answer the emitter is told. It found a defect one construct over: `Point { x: 1, x: 2 }` lowered, and `NK1172` refuses a field named twice in a literal as well |
| [119](adr-119.md) | **A target without an operating system.** Bare metal is a target with `user_parallelism` pinned to `no`: the state-machine model carries over whole, the OS half of `std` does not. `no_std` emission with abort; the target's executor with interrupts as wakers; an interrupt handler is a `fn() sync` that touches no lock, refused rather than trusted; a heap by default and an `allocation = "startup"` profile over a derived `allocates` column; a lock is a critical section the length of its block; runtime settings baked at build. Promises about time are stated with their limits, and no certification is claimed | Accepted | **no** — two targets exist, and `std` has one Rust half |
| [120](adr-120.md) | **A grammar's vocabulary is one page, an action is the block after the pattern, and two borrowed names go.** Part II 10.8 lists every element a grammar may write and is complete by rule; the one `->` a rule writes is its result type; `tag("x")` is `"x"` and `digit1` is `digit+`. Uppercase-is-lexical (ANTLR), `WS` (pest), `=>` and `# "…"` stay with precedent | Accepted | **no** — grammars write `-> { … }`, and the engine's names pass through |
| [121](adr-121.md) | **The ring's park hears the bell.** An eventfd on the ring, always armed, that a worker's reply writes to — so every worker operation can wake the executor: standard input suspends, `rt::io::wait` is awaitable, and the two completion paths stop differing in what they can feed. A hang is the failure this may not have, so the test is inverted and kept. A **poll** and not a read, which keeps `uring`'s soundness rule out of the bell; the descriptor lives beside the lock, because a worker that had to take it could not wake the thread holding it; the bell coalesces, so the park checks the fallback's own count inside that lock; and a park with a limit is bounded by `IORING_ENTER_EXT_ARG`, because after D1 an unheard bell would be a hang where there had been a panic | Accepted | **yes**, except `io::lines` — a step of it is an `Iterator::next`, which needs the `for` over a stream that is still open |
| [122](adr-122.md) | **A function-typed parameter lowers by its type**: the future shape unless it says `sync`, run or kept, so a pausing lambda fits a run parameter and the refusal goes. The box on the common case is measured before the record is closed, by [009](adr-009.md) D4's standard | Accepted | **no** — a run parameter lowers to `impl Fn` |
| [123](adr-123.md) | **`crosses` says *no* as well as *yes*.** `crosses = false` is the claim a type may not cross a thread, written by the describer from a foreign type's fields; absent stays *nothing recorded*, and a third spelling is refused rather than guessed at. The two crossing refusals can fire for the first time — [039](adr-039.md) D3's answer for `locks`, applied to the column beside it. `false` answers with arguments where `true` does not, which is the polarity: a promise is withheld where the line might not reach, a restriction kept. And it exposes a hole rather than making one — `NK2502` asks a call **nothing** describes, so a *described* foreign call is not asked, and no column says whether one puts what it is given on a thread | Accepted | **yes** — and the command writes it since [104](adr-104.md)'s step 2 landed: a `pub struct`'s fields decide the column, `false` for an `Rc` or a raw pointer, `true` where every field is plainly sendable, nothing where it cannot tell. D2's own list needed one correction, which a field reader is where it shows: a `Cell` is `!Sync` and not `!Send`, so a draft that wrote `false` for one would put a claim in the file that is false |
| [124](adr-124.md) | **`extern` and `unsafe` are reserved, because they cost nothing now and a boundary has to be visible.** Part III 15.1 writes C interoperability out and the language has none of the three things the example needs; two are words, and [084](adr-084.md) calls a keyword the most expensive thing a language adds. The number is **zero** — nothing in `examples/`, in `tests/` or in the three pages writes either as a name, and nothing is released. Reserved **with their constructs**, which is what [117](adr-117.md) has just undone for four words that had none. An `extern` declaration is `sync` and cannot `throw` without writing either: C has no suspension point, and a C function that sleeps **blocks**, which is `println`'s question and not this one. A call to such a name is written inside `unsafe { … }` and nowhere else (`NK1143`), because that is what the word buys — the boundary visible at the call. `touches` and `locks` are fail-closed, as a described crate's are. `Pointer[T]` is left open: a pointer that outlives what it points at wants a record with a lifetime story, not a name | Accepted | **yes**. The two words, both constructs, the ledger entries, `NK1143` and the lowering; `getpid` compiles and runs. §4's `Pointer[T]` is answered by [147](adr-147.md): a buffer is a view and a handle is opaque, and `malloc` stays unwritable by design |
| [125](adr-125.md) | **A library for other languages — `pub extern "C" fn` is an entry point, the caller owns the memory, and every call answers with a status.** `artifact = "c-library"` makes a package a library for C and everything that speaks it; the header is generated from the ledger and carries its hash. Numbers by value, text in as pointer and length, text out into the caller's buffer, a struct an opaque handle, an enum numbered in declaration order, an optional `NULL` or `NONE`. Every entry point returns an `int` status with a `throws` variant as a positive code and seven negative codes for the library's own failures; `<pkg>_last_error` carries site and secondary list. The caller may hand over an allocator before `init`; a panic is caught at the boundary and poisons the library until `shutdown` and `init`; a pausing function is exported blocking and as `_async`; a handle carries a lock and a re-entrant call is a status. Builds on [124](adr-124.md), which faces the other way | Accepted | **no** — `extern "C"` with a body is a parse error |
| [126](adr-126.md) | **`_` is the ignore pattern — it stands where a name would be bound, and nowhere else.** A tuple position, a parameter of a `fn` or a lambda, a `match` arm. `let _ = expr` is refused (`NK1144`) because the statement says the same thing; `_` is never a value. What it skips is not moved, so [094](adr-094.md) D4's view rule holds and a temporary ends with its statement. Lowers to Rust's `_`, and an ignored parameter produces no warning below. **It was already a name**, so `let _ = f()` compiled and Rust's `let _ =` *discarded* the value where the source said bound — which the record's own §5 said did not parse | Accepted | **yes** — `_` is no longer a name, and `_count`, `_0` and the `_000` of `1_000` still are |
| [127](adr-127.md) | **A struct crosses the boundary by value when it says `extern "C"`, and its layout is C's.** Declaration order and C padding (`#[repr(C)]` below); fields are numbers, `bool`, `char`, payload-free enums and other such structs, every field `pub`, anything else `NK1145` naming the handle as the shape. In, out and as a field by value; a `Vec` of them an array in and the caller's buffer out; `T?` of one the `NONE` status; no lock around its methods. The layout is in the ledger, so `--locked` catches a change. The same type serves a declared C function of [124](adr-124.md) | Accepted | **no** — `extern "C" struct` is a parse error |
| [128](adr-128.md) | **A symbol is named by the compiler**: the prefix is the package's name with `-` written `_`, or the one `symbol-prefix` line in `[build]`; a C identifier or refused. No declaration renames its own symbol, so the header stays a projection of the ledger | Accepted | **no** |
| [129](adr-129.md) | **An async call hands back a ticket that can be cancelled, and a stream is the callback the function already takes.** `<package>_op** op` ends the `_async` form (`NULL` for never); `<package>_cancel` cancels at the next pause point with `cleanup` run; `done` exactly once, `E_CANCELLED` (`-7`) when the cancellation came first; the ticket freed by the caller after `done`. Many results are `fn(item) -> bool sync`, `false` the stop and the return the backpressure; a returned list of text or handles is refused naming that shape. The blocking form has no cancel | Accepted | **no** |
| [130](adr-130.md) | **A WebAssembly library is the same entry point on another target — there is no `extern "wasm"`, and the host takes its buffers from the module.** `target = "wasm32-unknown"` with the library artifact makes `.wasm`, `.js` and `.d.ts` from the same declarations; `<package>_alloc`/`_free` for the host's buffers, no `set_allocator`, no blocking form; a pausing entry point is a Promise with an `AbortSignal`, the executor driven from the host's event loop; what the module may do is its target's `std` | Accepted | **no** |
| [131](adr-131.md) | **A binding is a generated file over the C library, written by `nikaia bind`, never a second artifact.** `python` over `ctypes` (exceptions per variant, `str`/`bytes`, classes with `close()`, `IntEnum`, `None`, generators for streams, an awaitable for `_async`); `js` is [130](adr-130.md)'s file. A binding never widens what the library exports; a native Node add-on, Java, C# and Go generators, and packaging stay open | Accepted | **no** |
| [132](adr-132.md) | **`else if` is an `else` whose block is one `if`, written without the braces.** No keyword and no new rule: the chain inherits the brace rule, the value rule and the `return` rule from `if`. The emitter writes Rust's `else if` where the `else` block holds one `if` and nothing else; the formatter keeps a chain flat. `elseif` is a **name** and `NK1117` says so, which is what *no keyword was added* means when the grammar is scannerless | Accepted | **yes**, except the formatter's line — `nikaia fmt` does not exist, so that is a rule for it to be born with rather than a step of this record |
| [133](adr-133.md) | **An argument list of options alone writes no `;`** — `execute(target_age: 30)`, `fn execute(target_age: i64 = 0)` — and the leading `;` is refused so there is one spelling; a mixed call keeps its `;`, required. One alternative tried first in the call rule and the signature rule, on the second token, because nothing in expression position begins with `name:`; no AST change. Part III 17.1 was right. **That last premise is the one thing the record has wrong**: Kap 4.2's `Stats(min: first)` is a struct literal with exactly that shape, so the two constructs share a spelling and resolution rather than the parser is what tells them apart — a question, in `open-decisions.md` §9 | Accepted | **the signature half**; the call half waits on that question. Two defects older than the record are fixed with it: `execute(; opt: 1)` lowered to `execute(, 1)`, and a struct literal naming nothing declared said nothing at all and is `NK1135` now |
| [134](adr-134.md) | **`/* … */` is a comment, it nests, and it is not a doc comment.** Anywhere whitespace may stand, across lines; an unclosed one is reported at its opening; `/** … */` is a comment. Doc comments are an open decision. One rule in `WS`, whose step tries the nested comment first; the opening is found by a scan, because the grammar fails at the end of the file and the `/*` may be thousands of bytes back | Accepted | **yes** |
| [135](adr-135.md) | **A list literal is `[1, 2, 3]`, the empty one takes its type from its first use, and a `[` at the start of a line begins a literal.** The grammar had no `[` in expression position at all, so every program built a list a `push` at a time. `[]` is [060](adr-060.md)'s rule one level up — the first use that needs a type gives it one, and one with no use at all is **refused** rather than defaulted, because a default element type is a type nobody wrote. An index across a line break is a shape nobody writes, so the ambiguity Rust and JavaScript both resolved the other way is resolved in favour of the literal | Accepted | **yes** — all four steps, with `NK1154` for elements that disagree and `NK1153` for an empty list nothing uses. D3 needed the grammar's one piece of `state`: the implicit whitespace is skipped before an alternative is tried, so a PEG has no way left to see the line break in front of a `[` unless something records it |
| [136](adr-136.md) | **A number literal may be written `1_000_000`, `0xFF`, `0b1010` or `0o17`, and the digits it was written in are a spelling.** The grammar is scannerless, so a separator was a misparse rather than a syntax error — `1_000` is `1` beside the name `_000`, which is `NK1117`'s standing example and stops being one. The radix carries **no width**: `0xFF` is `255` and takes the first type that holds it ([060](adr-060.md)), because a type that depended on how many zeroes somebody typed would make `0x0FF` a `u16` and `0xFF + 1` overflow where `255 + 1` does not. The separator is not in the value anywhere, a diagnostic's text included | Accepted | **yes** — written as an `extern rule`, because the DSL has no character class for a hexadecimal or binary digit and an action cannot fail. A number too wide for an `i64` used to `unwrap` and take this compiler down; it is refused now |
| [137](adr-137.md) | **`match` takes six more pattern shapes, a guard is `if`, and `..` in a pattern is inclusive — so `..<` is the exclusive range everywhere.** A tuple, an or-pattern, a range, a guard, a nested pattern and `..` for a struct's rest; `calc.nika` matches `step.0` today because it cannot match `step`. The sharp one is the range: this language's `..` is exclusive and Rust's pattern `..=` is inclusive, so either the pattern takes `..=` or `..` becomes inclusive everywhere. It becomes inclusive, and `..<` is the exclusive range in a `for`, a slice and anywhere else — **one meaning per spelling**, because the alternative is a rule the compiler cannot help a reader with: both forms parse in both places. Kotlin and Swift are the precedent. `..<` is never written in a pattern, and `..=` — which this language already had in an expression — goes with the change, because `..` is now what it said | Accepted | **yes** — the range spelling first, with D6's migration of every range in the tree in one change and a test that walks for one left behind, then the six shapes: a tuple pattern's parts became *patterns*, so nesting is recursion rather than a case, and the guard sits on the arm, where a guarded arm covers nothing. `NK1155` is the binding-set rule, and [146](adr-146.md) §4's parked question is answered — an or-pattern covers one case per variant it names |
| [138](adr-138.md) | **`throw`, `return`, `break` and `continue` are expressions of the never type.** `=> throw NotFound` is a parse error today and has to be written `=> { throw NotFound }`, where the braces hold nothing together and produce nothing. [093](adr-093.md) already gives the language the type they would have, so the type side was answered: never fits every expected type without widening anything, because an arm that throws hands back nothing at all. What they **do** is unchanged, and `NK1133` — a statement after a `break` — is what keeps this from turning `break x` into a quiet dropped value | Accepted | **yes** — and two things the record did not see, both [084](adr-084.md) D4's rule that a jump does not cross a function boundary: a `??` whose fallback jumps lowers to a `match` rather than to `unwrap_or_else`, which takes a closure, and `expr_stmt` normalises a bare jump back to its statement, or `NK1133` would have stopped firing |
| [139](adr-139.md) | **A doc comment is a language feature, and the ledger carries it.** `///` before an item is its documentation and the parser keeps it; the ledger gains a `doc` column on `pub` `fn` and `type` entries, derived like every other column, so a hand-edited one is overwritten by the package's own build. What asks for it is that `nikaia.contracts` **ships** and is the one file a consumer's compiler reads about a dependency — a comment in the source does not travel. Prose only: no `@param`, no directive, nothing the compiler reads. `/** … */` stays an ordinary comment, which keeps [134](adr-134.md) D3 true as written | Accepted | **yes** for an item, not yet for a field or a variant. The skip eats the `///` lines before a rule is tried, so the skip **records** them and the item reads them back — at its first byte rather than in its action, and against a **position** rather than a flag, because eight of `item`'s nine alternatives fail after consuming tokens. An ordinary comment between the prose and the item does not end the run; code does. `std` is the first corpus and it is complete: all hundred and nine hand-written entries carry prose, held there by a test rather than a habit |
| [140](adr-140.md) | **Five places where one thing had two spellings, and which one stays.** The struct literal is the **brace** form and `Name(field: value)` goes — which also unblocks [133](adr-133.md)'s call half, because `execute(target_age: 30)` and `Stats(min: first)` were the same five tokens. A type is built by its **anonymous constructor** and `new` leaves `std`'s own types. A path is **`::`** everywhere, the dot being for a value's members. `throws` and `sync` stand **after** the result type, which is the order [102](adr-102.md) D1 already fixed for a function type. `use` brings **nothing** in, for `std` as for a package. Five picks in one record because they are one question; five migrations in `open-work.md` because each is a rewrite somebody has to read | Accepted | **D1 to D4** — the named literal is out of the grammar, the corpus and the pages, `NK1146` names the braces, and [ADR-133](adr-133.md)'s call half is built on top of it; a promise before the arrow is refused with a message naming the order, and `sync` stands before `throws`; a grammar is entered through a path and the dot is `NK1147`; `Vec()`, `String()` and `HashMap()` are the constructors and a written `Type::new` is `NK1149`, with the ledger's key unmoved because that is what the lowering writes. D5 too: a `use std::…` naming a **type** is `NK1156`, and it turned out the compiler already did nothing with one — the line read like an import and was not one. What tells a module from a type in a ledger key is the `self` |
| [141](adr-141.md) | **Six things the specification writes are corrections, and three are constructs that stay unspecified.** `neg.is_some()`, a postfix `?`, `List[T]` for `Vec[T]`, a lambda carrying `sync`, a string where an enum is argued for, and an error raised as a positional constructor are **slips** — each has a spelling that exists today, so each is a page edit with no ruling behind it, and the last three are a page contradicting the page beside it. `5.seconds()`, `channel::bounded` and `select { … }` are **constructs** with no record: they are marked *unspecified* in place rather than removed or decided in a footnote, because each is a record's worth of question and Part I 8 is where it is taken | Accepted | **yes** |
| [142](adr-142.md) | **A grammar's action may not pause.** A rule's action is arbitrary Nikaia, so it could call something that pauses — and nothing refused it: `.await` went into the synchronous parser the `grammar!` macro generates and the backend answered about it. The demand is the one [ADR-050](adr-050.md) makes of an `overlap` branch and Part II 12.6 of a `par_iter` lambda, in the place a parser needs it, because a parse that can be cut into pieces and run on several cores ([ADR-009](adr-009.md)) is one whose steps do not wait on the world. So an entry is `sync` and says so in the ledger — which is what takes back the `async` that reaching an entry by name ([ADR-140](adr-140.md) D3) had spread through every parsing program. An action may still **fail** | Accepted | **yes** — `NK2209`, the column, and the leaf the fixpoint needed |
| [144](adr-144.md) | **A name denotes one thing, and the second declaration is refused.** Across files it already was ([ADR-047](adr-047.md) D1); inside one file nothing was, unless the build went through a manifest — and `nikaia --input` skips the module layer, which is how the corpus, the specification's blocks and a reader's first program are all compiled. So `struct Foo` beside `fn Foo`, two `struct Foo`s and two `fn f`s each lowered and `rustc` answered. `fn`, `struct`, `enum`, `trait` and `grammar` declare a name; a method, a rule, a field and a variant belong to something and do not. Found by building [ADR-140](adr-140.md) D1, whose `NK1146` had a help that was wrong for exactly the case this closes | Accepted | **yes** — `NK1148` in the checker, and two words on the list `modules.rs` counts |
| [145](adr-145.md) | **A `match`'s catch-all arm is `else`, not `_`.** The last arm is *everything else*, and it was written with a character borrowed from the ignore pattern — which says something different: [126](adr-126.md) is careful that `_` means *ignore a value that arrived*, and even argues against calling it a wildcard. Nothing arrives at a catch-all arm. `else` is what the same idea is called one construct over, is already reserved, and already reads as *the branch taken when nothing before it matched* ([132](adr-132.md)) — so this costs no word, which is most of the argument ([084](adr-084.md)). `_` keeps every other position [126](adr-126.md) gives it | Accepted | **yes** — `NK1150` from the parser, `MatchPattern::Otherwise` for the node, two arms in `examples/` |
| [146](adr-146.md) | **A `match` covers every case, and the refusal is this compiler's.** [137](adr-137.md) §4 left it open; it was never open below, because Rust refuses a non-exhaustive `match` — so the reader got the backend's words on a Nikaia line, which is C.1's class, and all this decides is whose message it is. Part I 3.4's own first sentence had been promising the rule. An **enum** is complete when every variant is named; anything else needs `else` ([145](adr-145.md)), because its set is not enumerable in arms — and `bool` is written out as the case that is neither. A bare name catches, as it always did. Where the scrutinee's type is unknown, nothing is claimed (C.4) | Accepted | **yes** — `NK1151`, naming the missing variants |
| [147](adr-147.md) | **The C boundary has a view and an opaque handle, and no raw pointer.** [124](adr-124.md) §4 left `Pointer[T]` undeclared on purpose, and that cost most of C: every function with a pointer in its signature — all of `libc`'s memory surface, every library that hands out a handle. The thing to prevent is the dangling **dereference**, and the two shapes real C libraries are made of each have a form that makes one impossible by construction: a **buffer** is a view that lives for the call, with its length checked against it at the call site; a **handle** is an `opaque type … released by …`, an address the language never dereferences whose release is a `cleanup`. `malloc` stays unwritable by design — memory the language indexes arrives with a length the language knows | Accepted | **yes**, all five. The four view forms lower to `*const T` / `*mut T` and are `NK1158` away from the boundary; the call makes the address (`bytes.as_ptr()`); a length beside a buffer that cannot be shown to fit is `NK1159`; a handle is `repr(transparent)` over the address with `Drop` for its cleanup, lent to every declaration but its release, and `NK1160` for a field, an index or a call reaching past one; a `CStr` is a handle with **no** cleanup and `std` copies it inside the one `unsafe` a program never writes. Part III 15.1's four blocks compile and run against libc, and §5's `sqlite3` is `examples/sqlite/main.nika` — which found that the four decisions are not four (a `T?` reaches a `T` through `?? throw`), a lint about a lower-case C type name, and `NK1161` |
| [148](adr-148.md) | **`select { … }` keeps the first branch to finish, and a task handle has `cancel()`.** Part II 12.4's block was *unspecified* ([141](adr-141.md) D2) while its semantics already existed — the loser stops at its pause point, its `cleanup` is adopted, the deadline bounds it ([006](adr-006.md) D3). Syntax and not a function, because an arm binds a name and then runs a block. It costs a keyword knowingly ([084](adr-084.md)): `overlap` runs its branches and keeps every result, `select` runs them and keeps the first, and a language with one and not the other has half a pair. `cancel()` costs nothing new, since the runtime already cancels a loser — so a program's cancel, a `select`'s loser and [129](adr-129.md)'s C `cancel` are one mechanism | Accepted | **no** |
| [149](adr-149.md) | **A channel is `std`'s, and only bounded.** Part II 12.5 needs no syntax — two values, two methods, and the tuple `let` is built. `send` **pauses** when the channel is full, so a `sync` body cannot send on one and the ledger's column says so without a rule about channels; `recv` hands back a `T?` and `null` means every sender is gone, which is the ordinary end of a stream and not a failure; the value type must `crosses`, checked where `tx` moves into a `spawn` as any move is. **No `unbounded()`**: a capacity is a promise about memory and this language makes programs say their promises — one that wants unbounded writes a large number and has said it | Accepted | **no** |
| [150](adr-150.md) | **A duration is `std::time::Duration`, written `5.seconds()`.** A `std` type and a `std` extension on the integers, beside the constructors for a computed count — which is what the pages already write. **No suffix literal**: `5s` would reopen for one type the form [136](adr-136.md) §4 declined for numbers, and a rule with an exception in it is one a reader has to remember rather than apply. The language learns nothing | Accepted | **no** |
| [151](adr-151.md) | **`break` carries no value, and the refusal names the two shapes that do.** [138](adr-138.md) §4 left it open; `NK1133` was already refusing it. A loop is a statement and hands back unit — one construct that is sometimes a value and sometimes not is the ambiguity `if` was spared by being an expression always. The message now names both shapes that carry a value out of a loop: a `let` before it that the loop assigns, or a `return` where the function has nothing left to do. Additive later, because every program that would use `break x` is refused today | Accepted | **yes** — one help text, and the decision behind a refusal that had none |
| [152](adr-152.md) | **A fixed-size array is `Array[T, N]`.** Two records point here: [127](adr-127.md) §4's C field `[f64; 3]` and [135](adr-135.md) §4's container that does not allocate, which [119](adr-119.md)'s `startup` profile needs on its first day. Rust's `[T; N]` is a new type form; this language writes every parameterised type in brackets already, so `Array[T, N]` with `N` a `comptime` integer adds no form. By value, indexed as a list with [114](adr-114.md)'s abort, and a literal takes the array type where the use asks for one — [135](adr-135.md) D2's rule for the empty list extended to a full one. The new part is an **integer** argument in the type language | Accepted | **yes**, except [127](adr-127.md)'s C field, which waits on that record |
| [153](adr-153.md) | **A native Node add-on waits for a program, and is generated C over the C library.** [130](adr-130.md) and [131](adr-131.md) give Node the WebAssembly build and left what it cannot reach open. Deciding now costs nothing and building now does not, which is the argument: nothing in this tree needs one. When one asks, `nikaia bind node` writes an N-API shim **in C** over the C library ([125](adr-125.md)) — generated source the host's toolchain compiles, as the Python binding is source the host's `ctypes` loads — and **not** a `napi` dependency in the emitted crate, which would make a second artifact and reopen *which artifact is the program* | Accepted | **no, and that is the decision** |
| [154](adr-154.md) | **The prelude is a written list, small and closed.** [140](adr-140.md) D5 said `Vec`, `String` and `HashMap` need no `use`, *which is the prelude and is unchanged* — unchanged from nothing written down: the line is drawn by a Rust `prelude` module grown one `pub use` at a time, and it carries `fs`, `io`, `cli`, `html` and `task`, so a program can read a file without saying so. The list is the containers (`Vec`, `String`, `Bytes`), the printing functions, `assert`, `panic` and the numeric conversions — **nothing that does I/O but printing, nothing that pauses**, which is also what makes it safe for a profile with no filesystem. `HashMap` is the first thing outside it, and a name joins by a record | Accepted | **no** |
| [155](adr-155.md) | **A handle may be absent, and `T?` is how it says so.** [147](adr-147.md) D3's own example is an out-parameter and a handle had nothing to be before the call; `fopen`, `getenv` and most of C's handle-returning surface return `NULL` and no declaration could say it. Part I 2.3's nullable, on a handle, meaning what it means everywhere else — and it costs nothing, because a handle holds a **non-null** address and `T?` is the absence of one, which are the two states C spells with a pointer and `NULL`: a nullable handle is one machine word and `&mut sqlite3?` is `sqlite3 **`. A declaration that does not say `?` is a **claim**, checked with an abort naming the function ([A.2](../30-nikaia-tooling.md)), which is the arrangement `sync` already has. `CStr::to_string` then fails in one way rather than two | Accepted | **yes**. The hull forced the declaration's *result* to change — a handle cannot be the declared return type, because a `NULL` in a non-null hull is undefined before any check could run — so the declaration hands back the address and the hull goes on at the call. D5 was found by building: `&mut Block?` sent the `?` to the view, and what C wants is a slot. [147](adr-147.md) §5's `sqlite3` then found that D1 needs `?? throw` to reach a `T` |
| [156](adr-156.md) | **`Bytes` is the language's, and the tether it needs is refused until it is built.** [154](adr-154.md) D1 put `Bytes` on the prelude's list and §4 left open whether it is `std`'s or the language's; enforcing the list closed the door, because what needs no `use` is what `std` keys **bare**. It is the **language's**: a type whose representation the compiler picks — Part I 6.6's three states — is not one a module owns, and `fs::read`'s result must be nameable without an import. One shared buffer (`Arc<[u8]>`), a count to hand on rather than a copy, and `fs::read` hands one back, which Part III 17.2 always said and the ledger did not. The mechanism it hangs on is **not built**, so a view of a buffer the body owns is refused on the Nikaia line — `NK2303`, naming the buffer — rather than lowered for `rustc` to explain ([C.1](../30-nikaia-tooling.md)) | Accepted | **yes**, but for the tether itself and `Mapped`'s deref. The refusal stands on a **named** buffer and never on a doubt: the column errs towards Tethered ([008](adr-008.md) D7) and a refusal may not, because refusing a correct program is the worse mistake ([C.4](../30-nikaia-tooling.md)). Nothing in the tree reaches it, held to over every `.nika` file |
| [157](adr-157.md) | **The failure channel is the error type, where the ledger names one.** [023](adr-023.md) D1's set has been derived into the ledger for a long time and [013](adr-013.md) D3 lowered every `throws` to one box, so D4's first row — *the variants within one error type are closed, and a `match` over them is exhaustive* — was unreachable through the channel. What it cost was Part I 7.1's **own** program: `match error { ConfigError::NotFound(p) => … }` over a `Box<dyn Error>`, which `rustc` refused while the page's Status note said *implemented* ([C.1](../30-nikaia-tooling.md)). Where the set has exactly one member and the member is a type this unit declares, the channel is `Thrown[E]` — the author's value plus the site D6 wants — and a handler is handed the error with the envelope opened once. Everything else keeps the box, each for a stated reason | Accepted | **yes**, but for the generated sum a set of two needs and for `std`'s own error names, which is what keeps every failing function in the corpus on the box. It also freed a second defect: a box is `'static`, so an error carrying a view of the caller's buffer — the page's own `ConfigError::NotFound(path)`, which [023](adr-023.md) D10 says should travel as a view — was `E0521`. And the derivation recorded a **variant** where one was written with named fields, so one error type arrived in the set twice |
| [158](adr-158.md) | **`std` names what it throws, and it is one type.** Every entry in `std`'s ledger wrote `throws = ["?"]` — [024](adr-024.md) D1's absence of a claim — so [023](adr-023.md) D1's set added nothing for the one library every program uses, and three records above it waited on the missing name. The measurement made it small: of 120 entries **nine** throw, two already name `Overtaken`, and the seven left all fail with one Rust type. So `io::IoError`, which the specification had already named twice ([023](adr-023.md) D6's worked output, Part III 13.5's example row). It lives in `io` and is written out — [154](adr-154.md) D3 straight, with the cost named: a program that only **passes** a failure on writes nothing, one that takes it apart writes the second `use` | Accepted | **yes**, and no lowering changed: [157](adr-157.md) D1 names a channel only after a type the unit itself declares, so every such function keeps the box and the claim moves first. Four variants, short on purpose because D4 of [023](adr-023.md) makes them closed — `NotFound` and `NotText` are the specification's own, `PermissionDenied` is the other failure a program acts on differently, `Other` is D4's named residue. The payload is owned text, which is where this falls short of [023](adr-023.md) D10 until the tether exists |
| [159](adr-159.md) | **A library's error type is a channel too, and it travels bare.** [157](adr-157.md) D1 named a channel only after a type **this unit declares**, which was right while nothing else could be named; [158](adr-158.md) removed the reason, and a program that read a file then had a set of one named member and still travelled in the box. Half was already built — a `catch` written directly over a `std` call binds what that call hands back — and what failed was the same failure **crossing a function boundary**, where `rustc` spoke about the generated file ([C.1](../30-nikaia-tooling.md)). A library's error gets **no envelope**: there is no `throw` in this program to have a site, and `std` hands the value back as it is, so propagation is the plain `?` and `error` is an `io::IoError` whichever way it arrived | Accepted | **yes**, and it is the first of the three error records to change what the corpus lowers to. `error.full()` says *no site recorded*, which is the sentence the opaque channel always used for an error from below. It also closed two path rules that read a **module** for a type — `io::IoError::NotFound` has three segments and the type is the first two — neither reachable before an error type lived in a module |
| [160](adr-160.md) | **A set with two error types in it is a generated sum.** Three records made a set of **one** a channel; a program reaches a set of **two** by doing two ordinary things at once — reading a file and throwing an error of its own — and `examples/sqlite` was already there. One `enum` per **set** and not per function, so two functions that fail the same way share a type and a `?` between them converts nothing; defined once at the crate root and named `crate::…`, because a module is a file of its own below. The program never writes the name: a `catch` matches on the **members'** variants ([023](adr-023.md) D4), so the lowering takes the match apart by member, with the catch-all written into each | Accepted | **yes**. Each member keeps the channel it would have had alone — the program's own type its envelope and its site, a library's bare ([159](adr-159.md) D2) — and `throw error` puts the failure back in the variant it came out of. A `match` over a `catch`'s error now needs `else`, refused twice: `NK1151` for the caret, and the lowering because it cannot write the file either way. One `rustc` warning had to be **allowed** rather than avoided, on a name spelled so nothing collides with it |
| [161](adr-161.md) | **Reading a map through the brackets, built — and what it took.** [114](adr-114.md) decided that `m[k]` on a map answers a `T?` and its §5 said nothing of it was built. This is that work, and a record of its own because three things it met were decisions [114](adr-114.md) did not make: `??` has to join a **view** with a **value**, which two non-overlapping impls do so that the emitter chooses nothing ([011](adr-011.md) D2) and no allocation appears that nobody wrote ([008](adr-008.md) D5); `panic` had to **exist**, because [114](adr-114.md) D1 writes it as the way a program says it knows a key is present; and a view of a `T?` had to be a nullable view | Accepted | **yes**. Two of the three were only reachable *because* of the change: `&m[k]` answered *unknown* and slipped past `NK1125` to `rustc`, and D2's own written-out counter `m[k] = (m[k] ?? 0) + 1` took `&m` inside the arguments of a `set(&mut m, …)`. The two corpus lines now say what they know — `… ?? panic(f"… was a key a moment ago")`, which is [114](adr-114.md) D1's own example |
| [162](adr-162.md) | **Diagnosis and safety are the language's — `eprint`, `access_all`, `update_all`.** [154](adr-154.md) §5 enforced the prelude's list in the direction a **program** writes, and left the other one unwatched: a name the compiler keys **bare** that the page does not list is one nobody ever meets. Three had drifted through, and Part III already disagreed with Part I about the first — *`eprintln` / `eprint` … **are in the prelude** rather than in a module*, against a list that wrote three of the four. `eprint` is diagnosis, like `panic` and `assert` beside it, and **what it does is not one thing**: a file descriptor on a hosted target, a host call on WebAssembly, a serial line or nothing at all with no operating system. A module cannot decide that, because a module does not know the target | Accepted | **yes**. `access_all` and `update_all` are on it under a reason of their own: Part II 12.3's doors over several locks buy that a deadlock **cycle cannot form** ([039](adr-039.md) D2), which is the language's rather than a convenience, and they are free functions only because a method has one receiver and these have several. The other direction now has the test it never had, read off the page; the reverse is not asserted while `assert` is listed and missing — a listed name that is absent is one a program meets, an unlisted one that is reachable is one nobody does |
| [163](adr-163.md) | **A block that joins on the executor pauses, and its branches travel in the function's own channel.** `overlap` and `select` were built before [055](adr-055.md) made the lowering implicitly `async` and before [157](adr-157.md) gave the channel a type, and neither was asked again: each had a correct program `rustc` refused, in a file nobody wrote ([C.1](../30-nikaia-tooling.md)). Ten lines were enough for the first — two branches that cannot pause left the function `sync`, and the vehicle's `.await` landed in a `fn` that is not `async`. Both lower to an `.await` whatever their branches do, so a body holding one **parks on the executor** and is not `sync`; neither is a **call**, so the shared resolution has nothing to answer and each analysis asks separately. And a branch's `Ok` names the enclosing function's channel rather than the box, because the `?` under the block converts into whichever of the four it is | Accepted | **yes**, but for D3. `throws` deliberately does not ask the same question: a block's failures are its branches', which that walk already reaches, and treating the construct as opaque there would put a `"?"` in every set — a claim about failures made to fix a fact about pausing. The **check** contradicts a written `sync` too ([027](adr-027.md) D4: an assertion is never overwritten), with a note saying where the pause is rather than naming a ledger entry that does not exist. `overlap { … } catch { … }` — [115](adr-115.md) D4's own form — still does not lower, and is named as a defect rather than fixed: combining at the block means looking at **every** outcome, which is that record's own first step |
| [164](adr-164.md) | **A joining block has one outcome, and a handler on it binds what the branches threw.** [163](adr-163.md) D3 named a defect rather than fixing it: `overlap { … } catch { … }` — [115](adr-115.md) D4's **own written example** — does not lower, because a `?` per branch leaves the *function* and the handler got a `match` over the vehicle's tuple. The branches become one outcome in `std` instead (`combine<n>`, written order, first `Err` wins — [050](adr-050.md) D5), which is also the only code that ever holds them all: where [115](adr-115.md)'s `secondary` list goes rather than a second copy of the same loop. And the handler binds the **branches'** set, not the function's, because the enclosing function usually is not `throws` at all and `match error { … }` over a box is not a program | Accepted | **yes**, all three. Measuring it turned one defect into four, and two need no joining block to reach: `catch { println(f"{error}") }` over a named channel was `unused_braces` (the envelope was opened as a block *around* the handler; it is its first statement now), and `fn f() -> String throws { throw E() }` was *unreachable call* (a tail `throw` writes its own `Err` and is not wrapped; a tail `return` is the other way round and stays wrapped). Held by a test that asserts `rustc`'s stderr is **empty**. What it does not decide: the `secondary` list, and where it can live when the channel is a library's type **bare** ([159](adr-159.md) D2 gives it no envelope) — which is the one question between this record and [115](adr-115.md) |
| [165](adr-165.md) | **There is no postfix `??`, and the three things one is reached for each have a spelling.** [018](adr-018.md) D3 writes `lookup(a.query("id")??)` — a postfix unwrap, which Part I 3.5 does not define, the parser does not have, and no rule anywhere states. It **breaks the operator it is spelled with**: `a ?? b` has a value on the left and a fallback on the right, and `a??` would not be that operator with an argument missing but an unrelated thing wearing its punctuation. And what the second meaning is, is an **abort** — on a value that turns out to be null the program ends, with a second question mark as the only thing in the source saying so. Part I 2.3's nullable exists so that *there is nothing there* is an answer rather than a stop, and everything on [A.2](../30-nikaia-tooling.md)'s list of what ends a program is written as a **word** | Accepted | **yes**. A fallback value is `a ?? b`; a jump is `a ?? throw NotFound` or `a ?? return r`, since the four jumps are expressions ([138](adr-138.md) D1); and *I know it is there* is `a ?? panic("…")` ([161](adr-161.md) D3), which ends the program with the program's own words. The refusal is where a reader meets the table — *expected one value, or an expression in brackets* is true and no help to someone who wrote the two characters on purpose ([C.2](../30-nikaia-tooling.md)). D2 is this record carrying the correction to [018](adr-018.md) D3's line, since a record is written once. `open-decisions.md` now has no open entry |
| [166](adr-166.md) | **Cargo's target-info probe is given an empty standard input.** Cargo asks every wrapper what the target looks like with `rustc - --print=…` — `-` meaning *the program is on standard input* — and writes nothing there. Passed straight through, that standard input is **whoever started the build**, and anything in it is read as a Rust program: *failed to run `rustc` to learn about target-specific information*, with a parse error at `<anon>:1` about text nobody offered as source. An invocation naming `-` **and** asking a `--print` is handed `/dev/null`; neither half alone is the probe, and a real compile names a file | Accepted | **yes**. `open-work.md` §3.1 carried this for months as *the wrapper's stdin is the suspect*, then recorded that suspicion **refuted** because `cargo test … < /dev/null` failed identically — right about what it measured and wrong about what it concluded, since closing the outer command's standard input does not close the **test binary's**, and the nested build inherits from the harness. A build that fails because of what was in the terminal's input is not reproducible, and the process that knows the probe's one correct input is the one holding the argument list. Held by the real probe through the real wrapper with a rendered diagnostic written onto its standard input, which reproduces the original error without the fix. §3.1 is **narrowed** rather than closed: a whole-workspace run still fails, and the reproducer is down to two binaries where what fails is Cargo package-cache lock contention — a different thing, which that entry now carries |
| [167](adr-167.md) | **The shared types are the language's, and a prelude name written with a module is refused.** [162](adr-162.md) D3 built the test that compares Part I 1.3's list against what `std` keys **bare**, and it watches **functions**: a type is keyed under its own name — `SharedMut::access` — so three walked past it. `Shared`, `SharedMut` and `Locked` are written with no `use`, which Part I 6.2 and Part II 12.2 have always shown, and were named nowhere on a list that ends *and there are no others*. They are the language's for [156](adr-156.md)'s reason one type over: which shape a shared value gets is decided **per value** by the compiler ([064](adr-064.md) D1), and a type whose representation the compiler picks is not one a module owns | Accepted | **yes**. Found by asking what happens to the *correct* spelling: `lock::SharedMut(0)` types as nothing, so Part II 12.3's nesting refusal — which [039](adr-039.md) D2 calls **always** — silently does not run. `NK1163` is the list read the other way: a name needing no `use`, written with a `std` module in front of it, which is the consistent guess because every *other* `std` name wants its module. Both halves are required — the module genuinely has no such name **and** the bare one is a name `std` keys — so it cannot refuse a correct program, unlike refusing on a module nothing describes yet ([140](adr-140.md) D5). The list's test now asks the **compiler** rather than the ledger, which is the right side |
| [168](adr-168.md) | **The re-entrancy check is built as a build option, and Part I 1.2's status note was wrong about two of its three clauses.** [039](adr-039.md) D8 decided the switch; measuring it first found the note describing it to be false. It said *nothing refuses the nesting of Part II 12.3, no re-entrancy check is emitted, and `nikaia.toml` has no key* — and `NK2203` refuses the nesting, the runtime check is in `std`'s crossing shape, and only the third clause was true. A stale status note is a defect in its own right, because a reader cannot tell a plan from a promise, and this one said a guarantee they have is one they do not | Accepted | **yes**. `reentrancy-check` is a `[build]` key, `yes` or `no`, `yes` by default, with a third spelling refused rather than guessed at and no command-line override — it *lives in the manifest*, because a shipped build that could be changed from the command line is not reproducible. It is a dimension of the build cache ([037](adr-037.md) D4) **and** of the compiled `std`'s own tree, which the other two switches did not need: the check lives in that crate, so without it the second project on a machine links the first one's answer ([021](adr-021.md) D7). Declining it reaches the crossing shape alone — the single-threaded shape's check is a `RefCell`'s own borrow flag, the language below's and not this option's to switch off |
| [169](adr-169.md) | **A type says what reading it touches, and I/O under a lock is refused.** Part II 12.2 says *no I/O while holding locked data*, and [067](adr-067.md) D1 split that in two — what **pauses** is `NK2202`'s, what **takes a lock** is `NK2203`'s — leaving the third case as a question to answer before writing a code. Measured: of the nine `std` entries whose touch set names I/O, four are the printing functions, four are `fs`'s reads and its write, and one is neither — reading a `fs::Mapped`. A mapping is a file held as memory, so `mapped[i]` is a **page fault**: a disk read that neither suspends nor takes a lock, and inside an open door nothing said a word | Accepted | **yes**. A type records what reading it touches in `touches`, on the **type** rather than on a function, because `mapped[i]` calls nothing a program wrote — the shape `iterates = "throws"` already has ([025](adr-025.md) D6). Silence is **not** a claim here, against this compiler's usual fail-closed reading ([010](adr-010.md) D1), because what is built on it is a refusal and refusing on doubt refuses correct programs ([C.4](../30-nikaia-tooling.md)). Both read shapes are covered, the message says why the other two codes are silent, and the way out is moving the read before the door. Free today: no program in the repository opens a door at all |
| [170](adr-170.md) | **An error carries the failures that joined it, and a body that joins puts an envelope on a bare channel.** [115](adr-115.md) D1 gives every error a `secondary` list and D2 fills it from an `overlap`; nothing about it was hard until the channel got a type. Four records later two of the four channels had nowhere to put a list — a library's error travels **bare** ([159](adr-159.md) D2) and a sum's members keep their own — and those two are the common ones. D2's reason was about the **site**, never the list: an `overlap` that combines failures is the language doing something, so there is something to attach even where nothing was raised here. A body that joins gets `Thrown<E>`, and the envelope says *no site recorded* | Accepted | **yes**, for D1 and D2 of that record. The list is of the channel's own type, because a joining block hands every branch the same one ([164](adr-164.md) D2), and each entry keeps its envelope and so its site — a secondary with secondaries is D3's tree with nothing further to build. An uncaught failure prints it indented, since a `main` handing back an `Err` is printed through `Debug` and a short form there is the one place the joined failures would be dropped. The boxed channel joins through a downcast, and a failure joining an error from *below* Nikaia is dropped — the one case the list cannot cover, named rather than discovered |
| [171](adr-171.md) | **A refusal from the lowering names its line.** [C.2](../30-nikaia-tooling.md) asks every diagnostic for a headline, the line with a caret, the reason and a way out; every `NK…` code gives all four and a refusal the **lowering** makes gave the first and the last. There are 33 of them, and the byte was there the whole time — `Flow::statement` carries it for the type checker's answers ([028](adr-028.md)) and it is the same number a caret wants | Accepted | **yes**, for the fifteen sites that have a flow in hand; the rest are about an item or a whole build, whose byte is a different walk. A helper with no place of its own — `interpolation`, which has a literal's text and never saw a file — borrows the statement's at the call, written once rather than at each site; a refusal about no statement keeps none, because inventing a line is worse than having none. The line is rendered at the **unit**, which is the one place with both the byte and the path — the lowering knows the statement and not the file, and whoever catches the error at the top knows neither. Deliberately the same shape as the checker's: a reader has no reason to know which half of this compiler refused them. No `NK…` number in front, because a number is a promise the rule stays and several of these are *this compiler cannot build that yet* rather than *the language forbids it* |
| [172](adr-172.md) | **A `for` may iterate something whose step pauses.** `for line in io::lines()` awaited the **call** and not the **step**: `Lines::next` was an `Iterator::next` over a `BufRead`, so a `for` over standard input held its thread for the whole loop — at `user_parallelism = yes`, a thread the pool could have had. A `Seq` says `pauses` after it, beside `sync` and `throws`, and the emitter writes `while let Some(x) = s.next().await`. **Three states and not two**: `sync` is a promise, `pauses` is a warning, and neither is *nobody said* — a `map`'s step runs the lambda, and the two ways of being wrong are not symmetric, since awaiting a step that has none is C.1's `rustc` about a file nobody wrote and C.4's correct program refused. The trait, when a second producer pauses, is `std`'s own: [055](adr-055.md) §5 rejects `tokio`, `tokio_stream::Stream` is `futures_core`'s re-export, and what a dependency would not shorten is the consumers. `io::lines` reads a **chunk** at a time, because a worker hop per line would be slower than the blocking reader it replaces The `for` and the **eager** consumers walk one; the lazy two are refused from the lowering with the loop as the way out, which also ended `io::lines().count()` counting the *failures* as lines — `Lines` was an `Iterator` over `Result[String, …]`, the ledger said `-> i64`, and the number was wrong with nothing saying so | Accepted | **yes** — the word, the lowering, the buffer, the refusal, and `examples/tally.nika` through a pipe end to end |
| [173](adr-173.md) | **What a parse fails with has a name.** A `pub` rule's ledger entry wrote `throws = ["?"]` because a parse fails with a **rendered string** and a string is not a type — the last `"?"` written anywhere in this tree, and the reason a program that opens a file and parses it could not tell *the file was not there* from *the file was not the shape the grammar says*. `ParseError` is that name: `nikaia-std`'s, in the prelude, keyed with **no module in front** like `Overtaken`, carrying the message and nothing else (fields are a second question nobody has asked). And naming it made a **miscompilation** visible: the `throws` fixpoint builds its graph from `Item::Fn`, a grammar's entry is not one, so what a parse threw reached **no caller at all** — invisible while a set of one `"?"` was the boxed channel, and `rustc` refusing the generated file the moment the caller had a named member from elsewhere | Accepted | **yes** — the type, the entry, the lowering, the fixpoint reading a non-graph callee's set, and `rustc` on the generated file, which is the only thing that says the last part is fixed |
| [143](adr-143.md) | **The database driver checks the SQL at build time against the schema — `std::db` is the protocol, the dialect is a package, and the compiler adds one intrinsic.** The compiler knows no SQL: `dsl sqlite(schema: app) { … } eod` hands the statement and a `comptime` schema asset to the driver's grammar, which refuses a missing column at the query and declares the result columns with `meta::column(name, type)`; the compiler derives the row type from them as it derives the parameter type from the holes. `std::db` holds only what two drivers must agree on. No expression capture ([088](adr-088.md) §3 closed by not adding it), no ORM; `raw(text)` for dynamic SQL | Accepted | **no** |
| [102](adr-102.md) | **A parameter may be a function, and the type says what it may do.** `fn(Request) -> Response`, with `sync` and `throws` after the result as a declaration writes them: without `sync` the code may pause, without `throws` it cannot fail, and a lambda that does less fits a type that allows more. Whether the callee runs or keeps the parameter is inferred — [094](adr-094.md) D2's `keeps`, asked of code — and a kept handler's promises are the type's where a run lambda's follow the lambda, as [029](adr-029.md) already has it. `@detached` is a ledger fact and never a word. What it opens is a package taking a handler, which is what `examples/http/` could not do | Accepted | **no** — the type is a parse error, and `std`'s lambda-taking entries bypass the grammar |
| [040](adr-040.md) | A handle on a shared value is duplicated, never moved, and each handle dies at the end of its own block — so there is no operation to name and no second meaning for `.clone()` | Accepted | **yes, both halves** — a handle handed to a **function** (a borrow duplicating nothing, `--sharing` naming each site) and one a **task** uses, the step written outside the future so the caller's own handle survives the `spawn`. The task half was unrunnable until `spawn` lowered and wrong for one commit after it. `NK2101` stays about data, which is what Part I 8.3 says |

### Grammar, DSLs, parsing

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [007](adr-007.md) | The scannerless grammar protocol and hybrid DSL binding; retires `unsafe asm` as a core construct | Accepted | D1–D4, D6, D8; D5's shadow type, its call-site check (`NK1112`/`NK1113`) and `...args: Self::dsl`, not its driver's `args.values()`. D7 decides *not* to build auto-AST and regex terminals, so it has nothing to implement |
| [009](adr-009.md) | Parallel parsing: frames, monoid folds, and where a format assumption is written down | Accepted | frames, `par_fold` |
| [011](adr-011.md) | Stage 0 lowering — the grammar protocol onto the parser backend, name for name | Accepted | yes |
| [016](adr-016.md) | The UTF-8 check is divided across frames, not skipped | Accepted | yes |

### Diagnostics

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [012](adr-012.md) | Errors are reported in the `.nika` file the user wrote, never in generated Rust. D8: a project build runs the binary itself, so Cargo never gets a second chance to replay a cached diagnostic against the generated file | Accepted | yes |
| [015](adr-015.md) | The backend's diagnostics are built lazily, because building them eagerly dominated the flagship | Accepted | yes |
| [044](adr-044.md) | One location table beside the program, and the panic hook looks the site up — so every abort names the Nikaia line, not the generated one | Accepted | **no** — the emitter keeps no line table and the panic hook is itself unbuilt |

### Trust and provenance

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [010](adr-010.md) | Input provenance: untrusted input is a property the compiler tracks, and an analysis that fails open is a vulnerability generator | Accepted | hasher, `--trust` |
| [017](adr-017.md) | Escaping is the template's contract, not the caller's discipline | Accepted | hole contract |

### `std` and the language surface

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [013](adr-013.md) | What Stage 0 refuses to infer; `std` is a crate, methods resolve through the receiver | Accepted (D5 superseded by [022](adr-022.md)) | yes |
| [014](adr-014.md) | `std` is written in Nikaia where it can be; `fs::map` maps, `par_fold` runs on rayon | Accepted (D1's *when* superseded by [002](adr-002.md) D4) | yes |
| [018](adr-018.md) | The HTTP handler sees the request, because a lambda already could | Accepted | no |
| [019](adr-019.md) | Standard input is a stream, read like everything else | Accepted | yes (`std::io`) |
| [022](adr-022.md) | One lambda form. `fn:` is removed | Accepted | yes |
| [041](adr-041.md) | Naming a lambda's arguments is the normal form; the automatic `a`, `b`, `c` are experimental and warned about where one is actually used | Accepted (D2's experimental carry ended by [049](adr-049.md) D1) | **yes, then removed** — the warning and the mechanism it shared with the emitter are gone with the form |
| [049](adr-049.md) | The automatic `a`, `b`, `c` are **withdrawn** — refused rather than warned about, and not announced a release ahead; what goes with them is the rule that read a lambda's arity off which of the three its body mentioned | Accepted | **yes** — `Expr::Closure` has no implicit flag, `implicit_params` and `NK1114` are removed, and a body reaching for `a` meets `NK1117`; eight sites in four programs rewritten |
| [050](adr-050.md) | Statement order is the written order: ADR-033's reordering, `seq` and the `ordering` switch are withdrawn, and `overlap { … }` is how a program asks for concurrency — with the touch sets kept to **check** the claim rather than to make one. D6 reaches the same schedule at both settings by starting every suspending branch before running one that cannot suspend | Accepted | not built: it needs the runtime binding `spawn` waits on |
| [051](adr-051.md) | This language's keywords are **reserved words**: a name may not be one of them. The grammar sublanguage's vocabulary (`rule`, `boundary`, `fold`, `par_fold`, `unchecked`) is not on the list, and after a `::` or a `.` a reserved word is a name, because no construct begins there. `self` is on the list and is also the receiver's name, so declaring one is `NK1119` from the checker rather than a parse error - except as a parameter, which never parsed at all and says so in a sentence now | Accepted | **yes** — and it closed three silent miscompilations (`if { }`, `let 5 = x`, `if a = b { }`, each read as statements about a variable named after the keyword) plus `let fn = 3`, which `rustc` refused about the generated file. Cost in this repository: two identifiers renamed |
| [052](adr-052.md) | `T?` is a type and `null` is a word, both lowering to the language below's `Option<T>` and `None`. A plain `T` stands where a `T?` is wanted — the one widening this language has — and the `Some(…)` is the **compiler's** to write, decided in the checker because whether the value is already nullable is a question about types and the emitter has none. `?.` reads the same way: `map` over a plain field, `and_then` over one that is itself nullable, or `a?.b?.c` holds a nullable of a nullable | Accepted | **all of it** — the type, the word, the wrap at an annotated `let`, an assignment, a `return`, a struct-literal field and a call argument, and `?.` choosing `map` or `and_then` from the field's declared type. `?.m()` on a *method* is refused with a sentence rather than built, because Part I 3.5 does not write it (§4) |
| [066](adr-066.md) | **`?.` reaches a member, and a method is one.** Part I 3.5's own word settles what [052](adr-052.md) left as the owner's question: a method call is reached like a field, with its arguments, its config zone and its trailing lambda, and the result flattens where the method's own result is a `T?`. It lowers to a **`match`** and not to the field's `map`, which is the one place the two members differ — a method may pause and may fail, and a closure is where neither can happen. And `??` chains, right-associatively, which was a precedence accident rather than a decision. **D6**: a plain `.` on a `T?` is refused (`NK1125`), because a `?.` guards its own member and no more and a `T?` is a type of its own — where other languages crash at run time this answers where it is written | Accepted | **yes** — `Expr::SafeMethod` is its own variant so no analysis can forget it, one `call_on` checks both spellings and one `method_call` writes them, and `tests/nullable.rs` *runs* four programs because short-circuiting is not a question a type check settles |
| [068](adr-068.md) | **A value whose type is not known is converted into the nullable slot**, rather than left alone. [052](adr-052.md) D4 writes `Some(…)` only where it **knows** the value is not already a `T?` — right, since wrapping one that is makes an `Option<Option<T>>` — and stayed silent otherwise, so the program failed in the language below instead. `value.into()` is correct in **both** directions (Rust has `From<T> for Option<T>` and the identity `From<T> for T`), so the question the checker could not answer stops needing one. `Some(…)` stays where the type is known, because it says what the source means | Accepted | **yes** — one `wrap_for` for all four positions, answering *how* rather than *whether*, and one `around` in the emitter. The test runs a file whose two unknown-typed values are a plain `String` and an `Option<&str>`: the same lowering wraps one and passes the other through |
| [069](adr-069.md) | **`http` leaves `std` and becomes a package**, reached by a path and living in `examples/` until its surface stops moving. The specification had been answering this both ways — the tooling chapter headed a section `std::http`, the first chapter used `http` as its worked example of a *foreign* package — and nobody noticed, because neither exists. Subtract what needs the compiler and nothing HTTP-shaped is left in `std`: the untrusted-path rule is [010](adr-010.md) D8's, escaping is `std::html`, a body the program never allocated is `fs::map`. What remains is the protocol and the framework, which age — and the direction is settled by which mistake stays cheap, since moving a package *into* `std` later is an addition and moving a module *out* is a break | Accepted | **the specification and the README**, which say one thing again; not the package — `std::http` never existed to remove |
| [070](adr-070.md) | **There is no unconditional loop keyword** — `while true { … }` is it, and Part I 3.3 now says the absence is a decision. Go is the precedent read carefully: it has no `while` **at all**, so what it shows is that *one* keyword carries every loop shape, not that the unconditional form is unnecessary. Rust's case for `loop` does not reach here, because `break` and `continue` are not in the grammar, the parser, or the 29 reserved words — so there is no value for a `break` to hand back, and the day there is is the day to reopen it. The divergence half is a checker improvement rather than a keyword: `fn forever() -> i32 { while true { … } }` is refused today and needs an unreachable `return` | Accepted | **the specification**; the compiler needed no change, and D3's unreachability is [093](adr-093.md), built once [085](adr-085.md) made it possible |
| [071](adr-071.md) | **`break` and `continue` are reserved words**, and neither is a construct yet. [070](adr-070.md) D2 found the first two are not in the language at all - not in the grammar, the parser, or the list - and [051](adr-051.md) D1's rule is about timing rather than taste: *reserving a word costs nothing before programs exist and breaks them afterwards*. `loop` is reserved although [070](adr-070.md) D1 decided against the keyword, because that no has a written condition for reopening and [050](adr-050.md) D7 recorded that un-reserving is the free direction - a reservation is reversible and its absence is not | Accepted | **yes** - 29 words to 32, a `RESERVED_C` alternation rather than two more arms on a tuple whose width forced the first split, and Part I 2.1's table, which turned out to be missing `null` as well |
| [072](adr-072.md) | **A file a build reads is named three times** - in the code as an `asset("…")` literal ([116](adr-116.md)), in an allowlist file one path per line, and in the invocation that puts that list in effect (`--allow-read-from-list=…`) - and **a build given no list reads nothing** at compile time. Two of the three are committed, so adding a read is a diff a reviewer sees; [021](adr-021.md)'s lockfile makes a new read visible *after* it happened and this makes it impossible *before*. The list binds the whole build, dependencies included, so a package cannot bring its own permission - the friction is the feature, and it is [026](adr-026.md) Q6's `build.rs` surface answered rather than recorded. No computed paths, no patterns: the property is that a read is decidable by looking at the line | Accepted | **yes** - `NK1175` is D1 and D3 with a reason per shape, `NK1176` is D4 and is answered **before** the argument is evaluated (a name that folds to the right path is exactly what it refuses), D7 puts the list's own digest beside the switches and what was read among the assets, and D8 is a warning at the end of the build. The `rustc` wrapper is told the same list through the environment, which is also how D6 holds |
| [073](adr-073.md) | **`const` is a declaration whose initialiser *must* be evaluated at build time** - the compiler already folds constants ([063](adr-063.md)), so the keyword adds a **demand** rather than an ability: *a `let` may fold; a `const` must, and says so where it cannot*. It stands where an item stands and inside a body, its type may be written and need not be, and it is a value rather than a place. What may stand in the initialiser is [026](adr-026.md) Q4's and is staged - literals and arithmetic now, a call and `dsl … from "…"` when Q4 answers - with everything in the second group refused by name rather than quietly run at program time. Measured for the record: a table known at build time looks up in **9 ns against a `HashMap`'s 18**, and the 3.9 µs of startup it also saves is *not* the argument | Accepted | **D1 and the body form of D2** - the 33rd reserved word, and `const` as a statement: it parses, folds, refuses what it cannot fold as `NK1127`, and reaches the language below as the value rather than the expression. Item level is not built, and D5's second stage - a call, and with it `dsl … from "…"` - is not either |
| [074](adr-074.md) | **A type parameter is a type inside its own body and a variable at every call site**, which is [024](adr-024.md) D4's erasure applied to the one place D4's reason holds. The `[T]` used to be read by the parser, erased by the checker and dropped by the emitter, so `fn hand[T]` asked `rustc` about a type nobody declared - [Part III C.1](../30-nikaia-tooling.md)'s class. Writing the `<T>` alone would have closed a third of it: the rest is binding from **arguments** ([031](adr-031.md) bound from the receiver, on purpose) and `NK1126`, which refuses a member on a parameter that has no bound rather than letting the backend say it. Measured, because it was asked: monomorphisation costs the program nothing at run time and `rustc` 11 % *less* than writing the copies out | Accepted | **yes** - `fn`, `struct` and `impl`, the ledger's `$T`, and nine programs |
| [075](adr-075.md) | **A build-time body may call what is `sync` and touches at most the build's parameters**, which answers [026](adr-026.md) Q4 with two columns that already exist rather than the restriction it asked somebody to invent. `cli::args` passes on purpose - same parameters, same code - so [005](adr-005.md) D8's byte-determinism gains a term and `Choices` gains a field; `println` is refused as a consequence of the boundary rather than a judgement about it. **No step budget for now**, and the cost is written down: `sync` says a body never pauses, not that it ends. With the restriction in place there is no sandbox, so Q1-Q3 close with it - no Wasm runtime, no `wasm32` target required of every contributor, no NaN asterisk on determinism | Accepted | **no** - `const` is not a declaration yet, and 24 `sync` ledger entries have no touch answer, so a `const` could not use a map (`open-work.md`, *nothing runs Nikaia code while the program is built* - by subject, because that page's numbers move) |
| [076](adr-076.md) | **A name the language below reserves is escaped where it is written**, and refused only where it cannot be. `let type = 3` lowered verbatim and `rustc` answered *"expected identifier, found keyword `type`"* about a file nobody wrote, with *"escape `type` to use it as an identifier"* as the help - [Part III C.1](../30-nikaia-tooling.md)'s class, in every position a name can stand in. **Escaped rather than reserved** (D1), because [051](adr-051.md) D1's rule is about words *this* language needs and `type` is the field name of every tagged record anybody has written; the cause is [011](adr-011.md) D2 writing a name for a name, so the fix belongs where the name is written. D3 is the one forced exception - Rust refuses `r#crate`, `r#super`, `r#self` and `r#Self` even escaped, so those three are `NK1128` | Accepted | **yes** - 25 words escaped, 3 refused at 8 positions, and a sweep that **compiles** every word in every position rather than asserting a list |
| [077](adr-077.md) | **The word is `comptime`, not `const`** - a keyword should say *when* and not *whether it changes*. [073](adr-073.md) took the name every neighbouring language uses, and it is wrong here for a reason only this language has: Part I 2.1 already makes every binding immutable unless it says `mut`, so a keyword meaning *this one does not change* distinguishes nothing. What the declaration promises is a **time**. `const` stays reserved rather than released, because [050](adr-050.md) D7's polarity says un-reserving is the free direction - and `const` is the first thing a reader from another language types, so a reserved word can be given a message where a name cannot. Nothing else about the declaration changes, and what the emitter writes is still Rust's `const` | Accepted | **yes** - the 34th reserved word, the statement's keyword, and six tests renamed with it |
| [078](adr-078.md) | **A trait is declared, and a bound is answered from the declaration.** Part I 4.7 writes `trait Summarize { … }` as its own example and it was a parse error at the keyword - a trait could be implemented and not declared, which [074](adr-074.md) §4 named as the prerequisite for `[T: Summarize]`. D3 is the reuse that made it small: a trait's methods are recorded as `Summarize::summary`, the same key shape an `impl`'s get, **because a bound and a receiver ask one question** - so the bound answers arity, argument types, result type, `sync` and `throws` from one entry rather than merely granting permission. D4 records a method with the words it was declared with, so *absent* reads as *may pause*, which is what a function type says ([109](adr-109.md)) | Accepted | **yes** - the word reserved, the item, `[T: A + B]`, and nine programs, one of which prints `User: Ada` |
| [174](adr-174.md) | **A bound is enforced where it is used.** [078](adr-078.md) made `[T: Speaks]` do one of the two things a bound is for — it put `say` in reach of the body, which is `NK1126` — while nothing asked the other, *which types may stand here*. So `tell(rock)` where `Rock` implements nothing was accepted, and the language below answered *the trait bound `Rock: Speaks` is not satisfied* about a file nobody wrote. One half of the question had no table: the ledger said `Speaks` **is** a trait and not that `Dog` answers for one, and that claim is made in exactly one place, the `impl` head. D1 puts it in the **ledger** and not in the checker's walk, which is the decision rather than the patch — an `impl` may stand in a different file from the `fn` that asks, and the first shape of this check refused a two-file program that builds and runs. D2's `NK1164` has two sentences, because `impl Speaks for V` is not a line anybody can write when `V` is the caller's own parameter | Accepted | **yes** — the ledger table merged across units, the refusal at both call paths, and the accepted program compiled and run |
| [177](adr-177.md) | **A grammar runs while the program is built by compiling the parser it generates.** The compiler holds the grammar tree, so walking it is the short path — and it is refused: `winnow-grammar` is a code **generator** with no interpreter in it, so interpreting here would be a second implementation of the same semantics and Part II 10.2's *one grammar, two stages* would stop being a property and become a hope. The disagreements would land in the corners — implicit whitespace, repetition bounds, the commit point, frames, interning, spans — as *this file parsed while the program was built and fails while it runs*. Instead the generated parser is compiled and run, so the agreement is a tautology. The cost is [026](adr-026.md) Q4's second compilation, keyed on what went into it. A result that cannot **cross** ([079](adr-079.md) D1) is refused from the rule's declaration, before a parser is built; invalid input fails the build in the parser's own words, relayed whole | Accepted | **yes** — and the measurement is the finding: **neither grammar in the corpus produces a result that crosses**, so the mechanism runs and [079](adr-079.md) D1 is what stops them, `NK1178` by name |
| [195](adr-195.md) | **`nikaia describe` is a Nikaia program, because this language has to be written in.** The command is 843 lines whose reading half is a hand-written character scanner its own module header apologises for; [193](adr-193.md) D4 said the answer was `syn`. D1 writes it in **Nikaia**, and the reason is the project's rather than the shape's: a language whose own toolchain is written in another one has no user forced to live with its gaps, and every gap this tree has found was found by **running** something. D2: nothing bootstraps through it - `NK2504` names the command and a person runs it - and it ships by [002](adr-002.md) D4's pre-lowering, *the `.nika` kept beside the `.rs`*, so there is no chicken and egg. D3 makes the reading half a **grammar** and replaces [193](adr-193.md) D4's `syn` step alone. D4 gives `std` a directory walk and a subprocess, on [194](adr-194.md) D1's own subtraction. **D5 is the one that outlives this record**: a measurement decides between options, and where the choice rests on a project principle a number is a **diagnosis**. The page this answers had proposed the grammar as a measurement that would settle the question - it would not have, and if the Nikaia version is slower that is work owed, not a reason to go back | Accepted | **no** — four steps, the first usable alone. The first tool written in the language, and the right first one because its output is **reviewed by a person** ([104](adr-104.md) D5) |
| [194](adr-194.md) | **Two servers, not one — the standard one is the operator's and an application's is its own.** The question forced a choice between *the program decides what is exposed* and *the operator decides*; the owner's answer is that the choice is **false**, and for a reason worth writing down: those are not two designs for one product but **two products**, each right about itself. A standard server (`nikaia serve [dir]`) has no author present at run time, so everything is the operator's; an application that serves has an author who decided already, and a tool that overrode them would take away the reason to write it. D1 puts the **socket** in `std` and the **protocol** in the package - [069](adr-069.md) D2's subtraction carried one item further, since a socket is an OS resource of the kind `fs` and `io` already own and a Nikaia package cannot syscall. D3 binds localhost, wider is asked for, and names [108](adr-108.md)'s *recorded per site and listed by `--trust`* as where that belongs. D4 routes with `.route(…)`, no `@route` and never *every `pub fn`* - `pub` is a package word and reading it as *the network may call it* is [010](adr-010.md) D1's polarity. D5 takes the smallest protocol a microservice needs and says the parser is **Rust until it is not**, so self-hosting is a step deferred rather than forgotten | Accepted | **no** — but the ground under it is: the runtime since [121](adr-121.md), the handler since [018](adr-018.md), and the whole chain **running** across a package boundary since 0.0.152. Nothing in the language changes |
| [193](adr-193.md) | **A description says whether it threads, and the describer proposes the answer.** `NK2502` asks its question of a call **nothing** describes ([038](adr-038.md) D7's own wording), so a *described* foreign call is not asked and `examples/foreign-runtime/crossing` is still refused by `rustc`'s `Send` bound - [005](adr-005.md) D7's open half. D1 gives a description a `threads` column with **three** values, hand-written like `crosses`: the absence is *nobody said*, never *it does not*, because a false silence is [010](adr-010.md) D1's vulnerability generator. D2 has `NK2502` fire on the **claim** and never on its absence. D3 is the half that makes a second hand-written claim worth having: the describer **proposes** and never claims. It may propose `true`, must never propose `false` - nothing a signature can show entails *does not thread*, since a function may spawn something it built itself - and [123](adr-123.md) D2's licence to fill `crosses` is **soundness**, which these indicators do not have. D4 reads the shim's two rows: in **safe** Rust the bound is forced and surfaces in the signature, so the scan carries that half alone; the call graph is for the row where an `unsafe impl Send` took the bound away. D5 flags `unsafe impl Send`/`Sync` - one pattern, sound, and the most useful sentence the tool can write about a foreign crate | Accepted | **D1-D5**, the record built: D1 and D2 at 0.0.160, measured at 0.0.161, the describer's two notes at 0.0.162, and D4's **call graph** at 0.0.163 — a body's calls from the grammar, the file's `use` items putting `spawn` and `tokio::spawn` back together, a private `fn` reported rather than skipped because the graph goes through it, and a breadth-first walk so the path a note names is the shortest one to check. `across_a_thread_unchecked` reaches `tokio::spawn` through `on_one_worker`, which is the row a bound cannot answer. D3: a parameter the bounds send gets a comment above its entry naming the evidence and asking the question, and never a column — `across_a_thread` gets it and `across_a_thread_unchecked` correctly does not, which is D4's own third row. D5: `unsafe impl Send for Smuggled<T>`, a promise made about a **private** type that every other step is blind to, and the note says what a tool cannot do about it. What is left is `cargo metadata`, which is [195](adr-195.md) D4's `std` subprocess rather than this record's. And the earlier half — the column takes `true`, `false` and absent, renders and re-parses, a third spelling is refused, and `NK2502` asks a **described** call where the word says `true` and nowhere else. All three ledgers under `examples/foreign-runtime/` now say it where it is true, and **both** crossing experiments are refused by this compiler rather than by `rustc`: `crossing/`, whose text was Rust's until now ([005](adr-005.md) D7's open half), and `smuggled/`, which `foreign-runtime.md` §3.5 did not expect — an `unsafe impl Send` defeats a bound and cannot touch a line a person wrote in a committed description. Not soundness, which D3 says out loud. The remaining steps are in `open-work.md`, each usable without the next. The macro limit stays and is not the parser's: expanding one needs `-Zunpretty=expanded`, which is nightly, the same [001](adr-001.md) D1 wall that keeps rustdoc-JSON out |
| [192](adr-192.md) | **The body's run-or-kept answer decides a code parameter's shape.** [122](adr-122.md) D1 made a pausing code parameter a boxed future *whether the callee runs it or keeps it*, on the ground that Rust had no stable `async` closure; [187](adr-187.md) measured that ground and found it false. The question was first filed at the wrong altitude - as *can the backend use a new feature* rather than *may one written type take two representations, chosen by an analysis of the body* - and read the second way it is not a new rule: `Shared[T]` is `Rc` or `Arc` **per value** ([037](adr-037.md) D7), [008](adr-008.md) D2 is *solved per construction site, not per type*, Part I 5.4 C says it about this very construct, and [094](adr-094.md) D1 already lets the callee's body decide the caller's `&`. D1: a **run** parameter is `impl AsyncFn(A) -> R` and the lambda an `async` closure with no `move` (Part I 5.4 A); a **kept** one keeps the box, because `AsyncFn` is a bound and a field needs a type; `sync` is untouched. 1.37 ns/call against 11.99, on a 0.31 floor. D2: run is the **absence of `keeps`**, so no column is added - [102](adr-102.md) D3 had already put it there. D3: the declaration and the call read that column the same way, absence included, because the two disagreeing is one parameter with two shapes. D4: what D1 traded away is predictability, and the ledger answers for it - the third use of an instrument, not a new one | Accepted | **yes** — `std`'s own entries are untouched, and the kept branch is written but not yet reachable: a field, a result and a `let` are `NK1142` until [102](adr-102.md) D5 lands |
| [191](adr-191.md) | **`??` joins two views, and a `?.` takes one of a binding and not of a temporary.** The owner answered [190](adr-190.md) D2's question with **A**, the option that page recommended - and building A in its literal form opened a new hole: a view of a **temporary** dies at the `;`, so `let name = find(1)?.name ?? "nobody"` is `rustc`'s *temporary value dropped while borrowed* about a file nobody wrote, the very class [113](adr-113.md) exists to close. And the obvious guard was the wrong one: `emit::is_a_place` asks whether an expression can be **written to**, which a `?.` reach can - `find(1)?.home?.city` passes it at every link and roots in a call. D1 answers on the **root** instead: rooted in a binding, the member is a view (Borrowed, [008](adr-008.md) D2's free case); rooted in a call, the reach takes the value as it did - which keeps [113](adr-113.md) D1's promise where it means anything, since a temporary has no line after to stay usable on. D2 is `NK1185`: a view on the left of a `??` and an owned value on the right asks for one value that is both, and the copy that would make them agree is [008](adr-008.md) D5's outright ban. D3 leaves the temporary to a **lowering** - `rustc`'s own *consider using a `let` binding* - which is not free because a `??`'s right side is lazy | Accepted | **yes** — the migration is the one line the recommendation predicted, and `NK1185` is free today because nothing produced a view on the left of a `??` before this record |
| [190](adr-190.md) | **The tether is needed at an escape and nowhere else - `?.` has no escape in it.** [189](adr-189.md) put the third of `?.` behind `open-work.md` §2.42, and the owner asked whether the tether is not only needed at very specific boundaries. It is, and [008](adr-008.md) D2 says so: **Tethered is chosen when the value escapes the buffer's owning scope** - returned past it, captured by a `@detached` lambda, written into a longer-lived container. A `?.` is none of those. D1 measures the four shapes: a local receiver, a temporary consumed in the same statement, and `view ?? view` (a literal *is* a view) all compile and run **Borrowed**; only a view bound past a temporary escapes, and that one is `NK2303`'s case, already built twice. D2 names what actually blocks it - a view on the left of a `??` and an **owned** value on the right, which cannot be joined without the copy [008](adr-008.md) D5 forbids - and files it as the owner's decision. D3 keeps [189](adr-189.md) D3's conclusion and replaces its reason, without rewriting the record. D4 writes down what the tether *is* still for, so this is not read as *not at all* | Accepted | **the correction** — the method it cost: [189](adr-189.md) reasoned from *a view of the receiver* to a **state** without asking which. The lattice has three and the cheap one is the default; a shape's state is a measurement, which is the rule [187](adr-187.md) had been written about one day earlier |
| [189](adr-189.md) | **`?.` lends its receiver where that needs no representation, and the third case waits on the tether.** [113](adr-113.md) D1 says `?.` takes nothing and Part I 3.5 already writes it; the compiler did not, and what a program heard was `use of moved value` with two hints naming `.as_ref()` and `.clone()` - neither of which this language has ([008](adr-008.md) D5 writes a copy and never inserts one), about a file nobody wrote ([C.1](../30-nikaia-tooling.md)). Read against the code, [113](adr-113.md) is not one piece of work: D2 gives the reach two answers, and the second - a member that does not copy comes out as a **view of the receiver** - is the state `open-work.md` §2.42 calls at least four change packages. D1 lends the receiver for a field the compiler knows to **copy**; D2 lends the scrutinee for a **method** that changes nothing, which needs no representation at all because what comes out is the call's own result. D3 leaves the third case moving and does **not** refuse it: a refusal is what stands where Tethered would everywhere else here, but this one would refuse `find(1)?.name ?? "nobody".to_owned()`, which compiles and runs today. D4 leaves Part I 3.5 stating the language and puts the gap where gaps go | Accepted | **two of three** — measured: lowering the third case as D2 asks makes `user?.name` a `ref String?`, and `?? "nobody".to_owned()` then has a view on one side and an owned value on the other, with no fit that does not insert a copy |
| [188](adr-188.md) | **The escape set is the language below's, written down once and refused here.** The parser takes `\` and **any** character and keeps both, and the emitter writes a `.nika` literal into the generated file verbatim - so what a `\` means has always been `rustc`'s answer and no page said so. `println("a\qb")` was refused on the right line ([012](adr-012.md) holds) in `rustc`'s vocabulary, ending *for more information, visit doc.rust-lang.org/reference/tokens.html* - a Nikaia program sent to the Rust reference to find out what it may write ([C.1](../30-nikaia-tooling.md), [C.2](../30-nikaia-tooling.md)). D1 puts the set on Part I 2.5 as **describing**, the bargain 2.2 already makes about numbers; the entry that carried this had said both that and its opposite four paragraphs apart, which is what held it open. D2 makes one table with two readers - the build-time decoder and the refusal - because two walks are two sets and the one that drifts open refuses a literal the other reads. D3 is `NK1184`, in the **checker** and not the parser: a `fail` in a lexical rule is not fatal here, so where the rest of the file parses the message is discarded with the attempt. D4 reads the way out off the escape: a malformed `\x` or `\u{…}` is told the form it should have had, anything else is a backslash to double | Accepted | **yes** — and it refuses nothing that existed: a sweep over every `.nika` in the tree passes, which is what says this is a translation and not a new rule |
| [187](adr-187.md) | **Rust has a stable `async` closure, and a premise under twelve sentences is false.** Twelve live sentences - a `std` module header, five emitter comments, three test doc comments, the roadmap, `spec-promises.md` and `open-work.md` §2.1 - said *"Rust has no stable `async` closure"*. Measured on this tree's own toolchain, stable, no feature gate: `async |x: i32| -> i32 { … }` and an `AsyncFn` bound compile and run, as do `AsyncFnMut`, `AsyncFnOnce` and `async move`. It was false when each was written, so nothing expired - it was never checked, which is [009](adr-009.md) D4 not applied to a shape's own premise. D1 puts the number in the tree: `benches/handler` gains an `async closure` row, and against a 0.31 ns plain-closure floor the boxed future [122](adr-122.md) D1 chose costs 11.99 ns and the shape said not to exist costs 1.37 - **8.7× less**. D2 replaces the *reason* where the conclusion survives: a task's body and an `overlap` branch are blocks and not callables, `rayon::join` takes **synchronous** closures, `Iterator::map` takes `FnMut`. D3 reopens [122](adr-122.md) D1, whose only surviving support is its own coherence half, as a question on [`open-decisions.md`](../../open-decisions.md). D4 leaves [055](adr-055.md), [033](adr-033.md) and [122](adr-122.md) unedited: a record says what was decided when, and a premise quietly corrected inside one no longer explains the code it produced | Accepted | **the correction, yes** — twelve sentences, the bench row and three rows in the table below. The shape D3 reopens is the owner's and is unbuilt by design |
| [186](adr-186.md) | **A parse keeps the text its record views.** [140](adr-140.md) D3 made a grammar entry a call by name, and three of its four columns have been derived since; `keeps` was left absent on the reading that *absent means nobody said*. It does not: `keeps_its` reads an absent `keeps` on a **present** entry as *keeps nothing*, so the column was answering **lends** for every parse in the corpus — including `Stock::file`, which hands back a record holding a `ref String` into the very text it was given. D1 makes a parse keep its `input` exactly when its declared result may hold a view into it; D2 reads that off the rule's **declared result** and not off the action blocks, because the input is not a name an action can reach; D3 keeps the text where the walk did not read a declaration for the result, which is [094](adr-094.md)'s own polarity; D4 writes the same fact into `views` as `input: borrowed` and `<result>: borrowed`, from **one** predicate, so the two columns cannot drift | Accepted | **yes** — and the entry it closes had cited a mechanism that was never needed: [`open-decisions.md`](../../open-decisions.md)'s `Bytes` question, answered by [179](adr-179.md) at 0.0.127 and gone from the file since. The walk existed, the predicate existed; what was missing was an item kind |
| [185](adr-185.md) | **A view meets two more constructs that were never told.** The two entries [182](adr-182.md) left open, and the same sentence one construct back. `for r in rows { let copy: Row = r }` answered *mismatched types* with *consider using clone here* — an instruction to insert exactly the copy [008](adr-008.md) D5 says is written and never inserted — and a grammar entry in **tail** position over a local that owns its input answered *`data` does not live long enough*, naming a local whose lifetime the source never mentions. D1 makes the first a **refusal** (`NK1183`) and not a lowering, because the alternative is that copy: the numeric half is already a lowering ([182](adr-182.md) D5) and inserts nothing, a `struct` is not `Copy`, and the way out is the annotation coming off — one the program can take, which is what makes it [C.2](../30-nikaia-tooling.md)'s shape. D2 binds the tail's value before the `Ok`, so the entry's block is a **statement** and its temporaries drop at the `;` ahead of the local — [164](adr-164.md) D1's `ARM_VALUE` trick one construct over, and `rustc`'s own hint. **Only where the lowering writes the borrow**, because it is the emitter's own `_source` | Accepted | **yes** — and it was the **tail** and not the grammar, which is why the corpus stayed green: the same call bound to a name first compiled, and so did the tail form over a parameter, because then no local owns the buffer. `tests/project.rs` builds and runs it, since the emitted text looked right and `rustc` is what refused it |
| [184](adr-184.md) | **A view is spelled `ref X`, and text has one noun.** `&` appears 98 times in the corpus's 2 847 lines and **82 of them are a *type* spelling, 53 of those `&str`** — only 16 are a borrow a program writes, because [094](adr-094.md) D1 already made a read-only parameter a view **without the word**. So the character a reader meets most is not a borrow operator: it is part of `&str`, the one place this language has **two nouns for one type**, which [107](adr-107.md) had to explain away in prose. D1 makes the assertion a word — `ref String`, `ref Array[T]`, `ref mut T`, `ref self`, `ref value` — and it **is** reserved — the corpus is not what decided that. Counting `ref` over every `.nika` file gave **zero** uses, which says the word is free, and it is not: `ref(x)` is a borrow of `(x)` to the grammar and a call to a function named `ref` to its author, in the same characters, and the reading nobody meant would be the **silent** one ([010](adr-010.md) D1's polarity). This record first said the opposite, on the argument that every position wanting a type or an expression after it makes the word safe; `crates/nikaia/tests/reserved_below.rs`, which writes each word in **every** position a name may stand, is what refuted it. A count over the corpus cannot see a collision the corpus does not write. D2 sends `str` back to being the **compiler's** word for what the language below calls it, normalised at **both** doors: with it at one only, the checker held `ref String` and `ref String` as two types and said so, which is worse than the `rustc` sentence it replaced. D3 makes `Array[T]` an array of **any** length and lets the **position** say where the length comes from: in a parameter the call says which and the function is generic over it (`[T; N]` below), under a `ref` the pointer carries it (`&[T]`), and in a field or a result there is no call to ask — `NK1182`, naming all three ways out. That is not two rules: owning needs a size and viewing does not, which is [107](adr-107.md)'s own arrangement for text one type over. D4 is where `&` leaves, with a note on the parse error so a program written the old way gets a way out and not just a verdict, and with a ledger still reading the old spelling — a `.contracts` file is a machine format this compiler reads, not a program a person writes. **No decision moves**: [094](adr-094.md), [107](adr-107.md), [008](adr-008.md) and [147](adr-147.md) all stand, and this changes characters | Accepted | **D1 and D2 at 0.0.133, D3 and D4 at 0.0.134.** The ADRs are not rewritten — a record says what was decided when it was decided. D3 was first written as a *refusal* of `Array[T]` and the owner corrected it to a **definition**: a run has no size only where nothing can give it one, and a parameter always can |
| [183](adr-183.md) | **A path's head is one of five things, and a name that is none of them is refused.** `Op::Mul` beside `enum Op { Add, Sub }` is `NK1171`; `nowhere::wobble` was not refused at all, and what the reader got was *failed to resolve: use of unresolved module or unlinked crate* — *crate* is not a word this language has, about a file nobody wrote ([Part III C.1](../30-nikaia-tooling.md)). The entry that carried it open had a good reason: a module of a package, an item of a foreign crate and a name no ledger was told about all look the same from inside the checker, so refusing on absence would refuse correct programs. **The measurement is what made it answerable** — instrumenting the path arm over the whole corpus found seven distinct paths used as values and **not one** with a head this compiler has not read. D1 writes the list down: a type, a module of this package, a package the manifest declares, a crate a description covers, a `std` module — all five already read, and the walk that asks them in order is what was missing. D2 answers for a described crate by a **prefix**, because a description is a list of its items and no table carries the crate's own word; without it `nikaia describe`'s output would be refused by the compiler that asked for it. D3's way out names a **file** three times over, which is the whole difference from *unresolved module*. D4 fails open, which is [010](adr-010.md) D1's polarity turned on a refusal: a missing one costs a `rustc` message and a wrong one costs a program. D5 is the file boundary, found the way [182](adr-182.md)'s was: `Shade::Even` in `main.nika` names an `enum` that stands in `shades.nika`, the tables D1 asks are this **unit's**, and every single-file test of the refusal passed while that program did not build | Accepted | **yes** — `NK1181`, the corpus unchanged, and the line held from both sides: a head nothing declares is refused, the corpus's own heads are not, and `io::wobble` stays quiet because what a known module holds is the ledger's question ([140](adr-140.md) D5) |
| [182](adr-182.md) | **A `for` lends, and everything downstream of the binding has to know it.** Two entries sat in [`open-work.md`](../../open-work.md) §1 with reproductions and both were [Part III C.1](../30-nikaia-tooling.md): `for n in NS { n as i64 }` answered *casting `&i32` as `i64` is invalid* and `text[1..3]` answered *the size for values of type `str` cannot be known at compilation time* — two nouns the program does not contain, each with a way out nobody can take. What makes them one record is that both are a **view this compiler wrote** meeting a construct that was never told: [094](adr-094.md) D4 lends at a `for`, [161](adr-161.md) D6 takes a read apart with a `*`, and over a **range** what it takes apart is already a view. D1 reads a cast's operand through the view by a **trait** and not by a `*`, because a `*` is right at the binding and wrong one line down where a `let` shadows it — `index::at`'s own reasoning, and the fifth uniform rule beside it. D2 drops the `*` at a range off the **shape of what is in the brackets**, so the emitter still knows no types. **And chasing them found four more**: a literal range `at` could not pin and a negative one that never reached the abort [048](adr-048.md) D1 promises (D3), a `let` that declared the element's type over a binding (D5), and the one that was in neither entry — `element_of` answered `?` for `Array[T, N]`, whose two arguments the `Vec` arm never reached, so `for n in NS { n as u8 }` over an array **truncated silently**, which is the one thing [043](adr-043.md) D4 exists to stop | Accepted | **yes** — `Local::lent` where `is_a_place` says the `for` lends, `num::value`, `element_of` over an array and a `&[T]`, and tests that **run**: the arithmetic has to be right as well as compile, and a shadowed binding is cast as itself in the same statement as one that is not. §1.6 opens for `let copy: Row = r`, which is a **refusal** this compiler owes and not a lowering it is missing — a copy is written and never inserted ([008](adr-008.md) D5) |
| [181](adr-181.md) | **`T::fields` is a list the build walks, and the loop over it is unrolled per type.** [088](adr-088.md) decided Part II 10.3 in full and built two of its six decisions — D2's `[T: Struct]` bound and D3's `NK1164` at the call — and *what the bound reaches* stayed `NK1171`, a refusal whose whole content was *this is specified and this compiler does not have it*. D1 gives the member a value: a sequence of reflected fields, each answering `.name` and `.of(value)`, and **a function walks a shape when its body says so** rather than when its bound allows it. D2 unrolls the loop once per type a call gave it — which is [088](adr-088.md) D4's own derivation, since `T::fields` is known while the program is built — emitting one copy and **no generic original**, with nothing left at run time but the field reads a program would have written by hand. D3 checks the body once per turn, because `field.of(value)` is a `String` for one field and an `i64` for the next and a body wrong for one is **right** for the others: every message carries *unrolling `T::fields` for `User`, at field `name`*, without which this is the error class C++ templates carried for twenty years | Accepted | **yes** — Part II 10.3's own example compiles and runs, which is the first time anything in that section did. `NK1180` for a member a reflected field does not have; `NK1171` narrowed to **`variants`**, which is a different value nobody has designed. D5 is [088](adr-088.md) D6's `--comptime`, beside the four reports that already existed: the same information the diagnostic carries, offered on demand instead of on failure, with a line of its own for a shape walk nobody calls — the one thing only the report can say, since no copy is written for it. D2 reaches **across the files of a package** since 0.0.130: both halves of it were read per unit, so a shape walk declared one file over and called from `main.nika` built into neither a copy nor a generic original |
| [180](adr-180.md) | **A fixed table holds a declared type as a *view* of one.** `comptime PAIRS: Fixed[&str, Row] = [("x", Row { a: 1 }), …]` was `NK1127` — *cannot evaluate* — for a value that evaluated fine and could not **cross**. The reason is `get`'s shape, and it is 0.0.118's own correction: `Fixed<V>::get` hands back `Option<V> where V: Copy`, because `Option<&V>` put a `&&str` in the generated file for a table of text and `??` over one is ambiguous. A Nikaia `struct` is not `Copy` — and **a reference to one is**, so D1 makes the table a `Fixed<&'static Row>` and nothing about `get` changes. D2 is the option not taken: deriving `Copy` wherever every field allows it would make a struct's copyability a consequence of its fields one file away, and would quiet the *used after a move* refusal [118](adr-118.md) D3 leans on — a copy is written and never inserted ([008](adr-008.md) D5). D3 is the door this opened onto the same place: a row holding a `Vec` lowered, so the walk that asks what a `const` cannot hold now reaches a **pair** | Accepted | **yes** — `declared_below` behind `rust_constant_type`, the rows spelled against their declarations so a `&[T]` inside one gets its `&` ([179](adr-179.md) D2), and a test that **runs**: a row looked up, a field read through `?.`, a `&[&str]` inside it, and a miss |
| [179](adr-179.md) | **`&[T]` is a type of this language, and it is what a run crosses as.** [079](adr-079.md) D1 said a build-time result arrives in its **view** form — `Vec[T]` as `&[T]` — and half of it was built: text crossed as a `&str`, a list crossed as an `Array[T, N]` ([152](adr-152.md)) *because the array is what the type language can already spell*, and `&[T]` stayed unspellable. Which was fine until the length was not one number: a **field** whose run differs per value has no `Array` to be, and `struct Section { settings: Vec[Setting] }` is what **both corpus grammars produce**. Two more absences wanted the same type — a binary asset has no result type an `asset_bytes` could have, and `Bytes` owns its run. The spelling already parsed, for the C boundary; what [147](adr-147.md) D1 added was a refusal away from it, on the reasoning that *a run whose length the type does not carry has no lowering here* — true of a **declaration** and never of a **view**, since `&str` is exactly such a run. So two writers for one spelling: an address beside a count at the boundary, Rust's own `&[T]` everywhere else. D2 is the line the two sides of it are easy to confuse: a **parameter** is where the `&` is the compiler's ([094](adr-094.md) D1), and a **field** filled from a run this body owns is `NK1179`, because a struct outlives the expression that fills it | Accepted | **yes** — the type, the crossing walked against the declaration, `Get` for `[V]` in `std`, the lend at a call, `NK1179` with a second sentence for a grammar action, and D3's sub-program that **owns** what the program views. `NK1158` is narrowed to [147](adr-147.md) D1's `mut` half |
| [178](adr-178.md) | **Tier-1 staging is withdrawn, and what would reopen it is a measured crossover.** [026](adr-026.md) §3 called compiler-side staging the tier that *waits on nothing and is where the interesting applications live*, and it went onto the roadmap as a `[ ]` in the language's count. [`staging-candidates.md`](../../staging-candidates.md) is the survey that followed, and **every candidate it names is closed** — three of them against its own prediction: the table-driven `html::escape` it recommended was **16 % worse**, a template's `with_capacity` is 0.0 % on a growing table, and first-byte dispatch for literal alternations — *the actual prize* — is under **3 %** of the parse, against **56 %** for the whitespace those alternations were re-skipping. So the box is not unbuilt work but a direction examined and not taken, and a `[ ]` nobody intends to open makes every honest one on the page worth less. D2 keeps the door a **measurement**: the survey's own rule, that a staging decision enters the compiler only together with a measured crossover, because the complexity is certain and the gain is not | Accepted | **the absence is the decision** — the box leaves the roadmap the way `loop` left Part I ([070](adr-070.md) D1), the language area is counted out of **25** rather than 26, and `scripts/check-roadmap-numbers.py` is what stops a withdrawal that forgets the number. The harness stays: it is why this could be closed rather than argued |
| [079](adr-079.md) | **Growable going in, fixed coming out** - a build-time result crosses in its **view** form, `Vec[T]` as `&[T]` and `String` as `&str`, which is the separation Part I already makes and happens to be where the language below draws its own line, so nothing has to be negotiated. What owns memory is refused **by its type** rather than by its parse. A map that crosses is **fixed** - and that is not tidiness: a closed key set is the precondition for a perfect hash, and the crossing is where it closes. So the lookup is the compiler's, the way [010](adr-010.md) already picks a hasher from key provenance; there is no `PerfectHashMap` to construct, because 200 keys favour a `match` **9.0 ns against 18.0** and a million do not, and a decision whose answer moves with the input does not belong in the source. A serialised blob is a **size** optimisation for later and admits no new types: two targets, two pointer widths | Accepted | **D1, D2 and D3** - the evaluator loops and pushes ([175](adr-175.md)) and the fixed map is [176](adr-176.md)'s `Fixed[&str, V]`, whose lookup turned out to need no emitted code at all. D5's blob is what is left |
| [176](adr-176.md) | **A map the build can see is a list of pairs, and what it crosses as is a table.** [079](adr-079.md) D3 decided the fixed map and left two things in front of it: the threshold, which [`fixed-map-lookup.md`](../../fixed-map-lookup.md) measured, and the **input** — a program builds a map with `HashMap()` and `insert`, both `std`'s with Rust bodies, and there is no map literal. D1 needs no new syntax: a list of pairs, and the **declared type** says it is a map, which is [152](adr-152.md) D4's *a use answers a literal* one shape out. D2 is the part a measurement turned around — the obvious lowering was a `match` for a small table, and a **linear scan over the same static arrays** turned out to be within 0–17 % of one up to twenty-four keys, past where the perfect hash has already won. So there is **no generated code at all**: a `Fixed` is a value of four `&'static` tables, the emitter stays one that knows no types, and the threshold is data in a `const` rather than a branch in the compiler | Accepted | **yes** — the type, the evaluator's tuple, the CHD generator, `NK1169` for a duplicate key and `NK1170` for a key that is not text, and a test that **runs** a program over both table shapes |
| [175](adr-175.md) | **A build-time value may be an array.** The evaluator could loop and could call, and had nowhere to put what a loop produced — so [079](adr-079.md) §3's table could be *computed* and had nowhere to arrive. D1 answers §3's *loop and push* without a `push`, and the answer is measured rather than chosen: `.push` hands back a `Vec[?]`, a `Vec` allocates, a `const` cannot hold one, and `NK1104` refuses it against an `Array[i64, 5]` before this evaluator is reached — so a table is written at its length and filled by index, which is what [152](adr-152.md) already made spellable. D2's `NK1165` is the index happening at the one moment there is no run to abort in, and it is said **once**: a refusal by name no longer drags `NK1127` behind it. D3 gives an `Array` a `len` in the ledger, which it had everywhere else — an unresolved `xs.len()` cost the enclosing function its whole touch set | Accepted | **yes**, and it fixed a **crash**: `comptime PRIMES: Array[i64, 4] = [2, 3, 5, 7]` reached `expect` with a word that had no diagnostic code and panicked on an `unreachable!` — a correct program, and Part I 6.8 met by the compiler on itself |
| [080](adr-080.md) | **An `impl` owes its `trait`, and a write through the brackets is not an index.** Two defects found by running the programs the specification prints, closed together because each had a shape the language below already gave: `NK1130` for an `impl` and a trait that disagree about which methods exist, `NK1129` for an implementation that pauses where the declaration says `sync` ([109](adr-109.md) D2) - and `index::set`, which is `insert` for a map and an indexed assignment for a sequence. Rust's `Index` for a map is over what the key **borrows** as, so `scores["Player1"] = 100` pinned nothing and Part I 4.5's own example failed with a message naming `TrustedMap` and a type parameter `K`. A trait and not two emitter cases, which is [048](adr-048.md) D1's sentence a second time: this emitter does not know types | Accepted | **yes** - three refusals, one `std` trait, eleven programs |
| [081](adr-081.md) | **A `+` over text is a call, and an expression gets a span to key it by.** Three of the four ways to join text went below and were refused there - Rust's `String + &str` rule reaching the user unchanged - and the checker typed `"a" + s` as a `&str` first, which made `-> String` a **false** refusal, the direction [Part III C.4](../30-nikaia-tooling.md) forbids outright. D2 is a trait with an impl per shape, which is [048](adr-048.md) D1's sentence a third time and costs nothing: `String + &str` stays at 42 ms where a `format!` for every shape would be 250. D3 is why it is narrow - a number's `+` may not move into `std`, where `overflow-checks` is off and inlining does not carry the abort across ([043](adr-043.md) D1), measured | Accepted | **yes** - the first expression-level span, `std::concat`, and nine programs |
| [082](adr-082.md) | **A grammar is entered by a call, and every `pub` rule is an entry** - `Json.value(input)` rather than `dsl Json from input`. `dsl` spelled two constructs with nothing in common: the **block** form takes a *driver* name and is untouched, the `from` form took a *grammar* name - one keyword, two namespaces. Worse, the same line meant two things by position: at run time it parsed the eleven characters `config.json`, in a build-time binding it was meant to parse that file's contents. The distinguished entry was ours and is what produced the defect - the emitter takes the **first** `pub` rule, and a `par_fold` one beats an earlier one, silently; the layer below has no main rule and generates one function per public rule. No `parse` alias: a name that appears only while a grammar has one public rule is worse than none | Accepted | **yes**, in one change - `Grammar.rule(expr)` is the entry, every `pub` rule is one in the ledger with `throws`, `entry_rule`'s silent source-order choice is deleted, and `dsl X from e` is refused with the call in the message. Eight programs, the specification's own blocks and three test files migrated with it |
| [083](adr-083.md) | **A field of a borrowed subject may not be handed out by value** - `NK1131`. `return self.username` out of a `&self` method was `E0507` about a file nobody wrote, which is every accessor over a field that is not a number. **Part I 6.8 decides it and had already:** *ownership rules occasionally reject code, every such error explains itself in plain language, and a raw internal error reaching you is a Nikaia bug* - both halves were being broken at once. Not a `.clone()` the emitter writes, which is against [064](adr-064.md) D2's *a hull you can see is one you write* and would buy nothing, since both ways out are one word. Measured as a class first: three positions leak and three work | Accepted | **yes** - four call sites and six programs, most about the direction that must not fire |
| [084](adr-084.md) | **`break` and `continue` are in the language**, unlabelled and carrying no value. [070](adr-070.md) D2 found the absence and left it open; [071](adr-071.md) reserved the words so it could stay open for free; this answers it, and the construct was **built and measured before the record was written**. What decided it is not a percentage: a `for` here **cannot be stopped** — `return` leaves the function and a flag stops only the body — so the shape that replaces a `break` takes two million turns to do 1,415 turns' work, unbounded in `n`. D3 refuses `break x`, because a value written after a word that takes none parses as a second statement and is dropped in silence. D4 refuses a jump whose loop is outside a lambda, a task, an `overlap` branch or a fold's step, each being a function below — and D6 puts the same refusal in the **lowering**, because a walk cannot answer whether it is complete | Accepted | **yes** — 435 lines, 18 tests, and six analyses that each gained an arm doing nothing, which is D2's argument for having no labels arrived at from the other side |
| [085](adr-085.md) | **The unconditional loop is lowered to `loop`**, and the preamble gets no `allow`. [084](adr-084.md) made `while true { … }` a shape somebody would write, and the emitted Rust then warned *"denote infinite loops with `loop { … }`"* about a line nobody wrote — which [084](adr-084.md) §3 recorded as a defect whose fix was one `#![allow]` in the preamble. It is not: a blanket suppression says *"nobody will be shown this again"* rather than *"we looked"*, and the warning was `rustc` being **right** — `while true { }` is `()` below and `loop { }` is `!`, so a function that never returns can only be built out of the second. The general rule: where the language below complains about emitted code, ask first whether it is correct | Accepted | **yes** — one arm in the emitter on the condition being the literal `true`, three tests, and the `while_true` warning gone from the whole corpus |
| [086](adr-086.md) | **A head parses the same language as a body, minus the forms a `{` begins** — so `&&` and `||` join it, and `if a && b { … }` stops being a parse error. The head chain exists because a `{` there opens the body; it was built by copying the ordinary chain and stopping too early, and what stopped was not only the brace-led forms. The rule matters more than the two rules: a head that differs in any *other* way is a trap rather than a restriction, because the program reads as valid Nikaia. §3 is the part to read — the same benchmark, with the shape this made writable, says a flag costs **exactly** what a `break` costs, so [084](adr-084.md) §4's `while` row had been measuring *this* gap. Its decision is untouched; what decided it was the `for` row | Accepted | **yes** — two levels, four tests, 2,536 instructions a head, and the `open-work.md` entry on the connectives closed |
| [087](adr-087.md) | **A head holds every expression; only a brace-led one needs parentheses.** [086](adr-086.md) D3 wrote the rule and D4 made an exception to it for `??`, on the grounds that no program had run into it. Asked what *else* the head refused, the answer was four forms — `??`, `as`, `null` and a tuple — and `null` says what kind of defect that is: not a precedence level but a **literal**, in a head that already took `false`. **A reproduction is the right evidence for adding a construct and the wrong evidence for an inconsistency in where an existing one may stand** (D3): waiting on the second does not gather evidence, it just means the next person meets it. The restriction that remains is about the token that opens a body, and parentheses reach past it — so it is a rule about spelling, not about meaning | Accepted | **yes** — the chain mirrors the ordinary one level for level, five tests, and 5,016 instructions a head at 0.5–1.2 % of a compile |
| [088](adr-088.md) | **Reflection is data, not a syntax tree** - and `macro` and `quote` are names, with `with` reserved for a construct of its own ([117](adr-117.md)). Part II 10.3 described a macro system with three constructs; the problem it names - generating code from a type's shape - is solved with none of them, because `comptime` plus a **bound** is enough. Zig's version of the same example is an ordinary function and its absence of macros is deliberate, on a readability argument; D went the other way with string `mixin`, which is [Part III C.1](../30-nikaia-tooling.md)'s class. Here the bound is what makes `T.fields` exist ([078](adr-078.md)), so the trait system does the navigation Zig does with a union tag - no builtin, no `anytype`, and no second `inline for`, because a loop over something build-time-known cannot be anything else. The body is checked **per unrolled iteration**, which costs nothing at run time and demands that the diagnostic name the unrolling; what cannot be read is answered by `--comptime`, a report rather than syntax | Accepted | **D2's bound, D3 and D7** - `[T: Struct]` and `[T: Enum]` are bounds answered by the **declaration** rather than by an `impl`, which is D2's whole point, and D3's *you passed something that is not a struct* is refused at the call. The bound does not travel into the generated file, because Rust has no trait by either name. Running the example used to name three missing things deepest-first; two are gone - the build-time loop arrived with [175](adr-175.md) and [176](adr-176.md), and `Struct` is a bound now - and what is left is the **shape as data**, `NK1171` where it is written |
| [089](adr-089.md) | **A `??`'s fallback is one value, or an expression in brackets.** `??` sat above the whole binary chain, so `a ?? 0 > 3` was `a ?? (0 > 3)` while looking like `(a ?? 0) > 3`. `open-work.md` filed it as a **suspicion** with the exact question that would decide it — is there a shape where both readings type-check — and the answer is yes: with `a: bool?`, `a ?? x == y` is `false` where the line looks like `true`. A silent wrong value. **Refused rather than re-precedenced**, because Swift reads it one way and C# and Kotlin the other, so any precedence misreads for somebody and gives them no message; and a narrowing can be relaxed into a precedence later while a precedence cannot be un-chosen. Costs nothing today — every `??` in `examples/` has a simple fallback | Accepted | **yes** - a grammar rule, a note that says why, and five programs |
| [092](adr-092.md) | **A grammar fold's `init`, `step` and `merge` are code**, walked by the ordinary expression walk. They are expressions inside a **pattern**, and the grammar walk took a rule's *action block* and nothing else - so `fn(acc, m) { nothing_declares_this }` lowered without a word and `rustc` answered about a file nobody wrote. [084](adr-084.md) D4 had closed the half a **jump** can reach and said why it stopped: walking them with the whole checker might newly refuse programs. **It refuses none** - measured over the corpus, where `1brc` and `access-log` both write a `par_fold` with a path in two positions and a method on a `?` in the third - so D4's own walk is **removed** rather than kept beside this one, because `Expr::Closure` already crosses a boundary and counts loops from zero. The jump's message improves in the bargain: the old walk had no barrier set and offered `return`, which inside a grammar action is wrong advice | Accepted | **yes** - one walk where there were two, the dead one deleted, and seven programs |
| [093](adr-093.md) | **A loop that cannot end needs no unreachable `return`.** [070](adr-070.md) D3 decided it and named it the one real cost of D1's *no second keyword*; `fn forever() -> i32 { while true { … } }` was `NK1104`, so an accept loop had to end with a `return 0` nobody reaches and no reader can tell from a mistake. **Not buildable before [085](adr-085.md)**, whatever the checker did: `while true { }` is `()` below and `loop { }` is `!`, so the refusal would only have moved to `rustc`. The test is the literal `true` **and** no `break` bound to this loop - the second half is [084](adr-084.md)'s, and this entry's own earlier version had argued the work was small *because* `break` did not exist. The walk over-approximates *a jump leaves it* on purpose: a false yes asks for the `return` this removes, a false no hands `rustc` a file nobody wrote | Accepted | **yes** - one `filter` at the tail, one walk, and four programs |
| [099](adr-099.md) | **The two refusals that come with the lock's doors.** [039](adr-039.md) D10 gave shared mutable state four doors and named the two mistakes that come with them; Appendix C catalogued both and **nothing raised either** - `open-work.md` §2's own *state that rots fastest*. `NK2204` names the door **with the value in it**, because the repair is mechanical and *"use a door"* leaves the reader to work out which of four; what cannot be rebuilt from the AST is an ellipsis rather than a guess, since an expression has no span ([081](adr-081.md) D2). `NK2205` is **the `get` written *inside* the `set`, which [111](adr-111.md) widens by the stamp a value carries out of a lock to the pair spread over two lines or two functions. These two and not the other three, because `NK2201`, `NK2203` and `NK2503` need what a door contains and what a call chain reaches, and two local rules beat five half-analysed ones | Accepted | **yes** - two refusals, the emitter's walk shared rather than copied, and seven programs |
| [098](adr-098.md) | **A `let` takes one name, or a flat tuple of them.** Part I 8.1.2's `let (user, rights, prefs) = overlap { … }` and Part II 12.5's `let (tx, rx) = …` are a form the specification uses twice and defines nowhere; `overlap` met this rather than made it, and was reached by its tuple in the meantime. **Not a pattern language** - `match` has patterns and neither site needs them. One field on one variant rather than a second statement, so all nineteen readers had to look, and **three of them decline**: `contracts::order` goes opaque rather than reorder a statement binding several names, `contracts::sharing` records no handle so each part stays at [037](adr-037.md) D6's atomic floor, and `dsl` skips. The same polarity three times - an analysis that cannot answer says nothing, and each fails safe. Widths are not compared, because a value this compiler cannot see is `?` and refusing that would be [Part III C.4](../30-nikaia-tooling.md) | Accepted | **yes** - a grammar rule, `NK1136`, a parse-error sentence, and seven programs |
| [097](adr-097.md) | **A `comptime` binding where an item stands.** [073](adr-073.md) D2 decided **both** places and built one, and `Item::Comptime` had been in the AST since - with its `public` field - while nothing produced or consumed it, which is a decision that looks built from the inside. What the item form needed and the body form did not is a **frame under every body, filled before the item loop**: this checker's scope is a stack pushed per function, and a function declared *above* a constant sees it as much as one below, because an item is visible in its whole scope. One function under both places, since D2's own sentence is that the difference is *where the name is visible, never what may stand to the right of the `=`* - so the fold, `NK1127` and the spelling the emitter is handed are one thing in one place, and a place where `NK1127` did not fire would be a place where `comptime` quietly means `let` | Accepted | **yes** - a grammar rule, a pass, an emitter arm, and five programs |
| [096](adr-096.md) | **A type nothing declares is refused rather than lowered.** A **value** nothing declares has had `NK1117` since [051](adr-051.md); a type had nothing, so `let x: Widgit = 3` went to `rustc` intact and came back as *"cannot find type `Widgit`"* - [Part III C.1](../30-nikaia-tooling.md)'s class holding for one half of this language's names and not the other. [064](adr-064.md) fixed one such name by **building** it and left the class. The known set is **derived rather than listed** - `OFFERED`, the ledger's `types` under both the qualified key and its suffix, and the declaration's own parameters - because a list beside those is a second thing to forget and forgetting it refuses a correct program. A **qualified** name is left alone, since whether this build sees that package is [046](adr-046.md) D2's question. The measurement is the point: one line in the corpus (`fortunes.nika` wrote `List`, a name the language does not have) and four specification blocks that were **already `error[E0425]` below** - the refusal moved from the backend into this compiler | Accepted | **yes** - `NK1135`, a walk of its own over ten positions, and six programs |
| [095](adr-095.md) | **A trait reached across a package is imported by the emitter.** Rust needs a trait **in scope** before its methods can be called, and `impl http::Handler for Fixed` does not put it there - so a trait a package publishes could be implemented and then not called, in [Part III C.1](../30-nikaia-tooling.md)'s words twice over: the backend's message about a file nobody wrote, whose remedy - *"perhaps you want to import it"* - is one the program **cannot write**, because [046](adr-046.md) D2 gives Nikaia no import at all. The same shape **in one file compiles**, which is what said this was one missing emitted line rather than a question. Qualified is the whole of the test: a `::` in the name is exactly what says the declaration is in another crate, so a trait of this program's own and a trait the compiler reads ([023](adr-023.md) D3) are both left alone without the test knowing about either | Accepted | **yes** - one set on `Needs`, one loop in the preamble, and a two-package program that runs |
| [091](adr-091.md) | **A `catch` over an expression that cannot fail is refused.** The lowering makes a `catch` a `match` over a `Result`, so a guarded expression that is not one got `E0308` about the generated file - [Part III C.1](../30-nikaia-tooling.md)'s class, naming an `Ok` arm nobody wrote. [025](adr-025.md) D1's `NK2605` already asks this question from the other end, and its rule carries over: **the ledger is the fact**. So the walk records two answers rather than one - *something here can fail* and *something here could not be looked up* - because [Part III C.4](../30-nikaia-tooling.md) makes only the first actionable: the refusal fires on **known not to fail**, never on *not known to fail*. **Refused rather than dropped**, because emitting the expression without the `match` deletes a checked block from the program, a `return` inside a handler included. The corpus is what found D4: six examples write `dsl … from … catch { … }`, which can fail by [023](adr-023.md) D9 and carries no contract to say so | Accepted | **yes** - `NK1134`, two booleans on paths that already existed, and six programs |
| [090](adr-090.md) | **A `catch` that ignores the error binds `_error`.** Kap 7.1 gives the failure the name `error` whether or not the handler reads one, so `catch { 1000 }` — the shape Part I 7.1 teaches first — reached the author as `warning: unused variable: error` about a binding that exists nowhere in their program: [Part III C.1](../30-nikaia-tooling.md)'s class one severity down, and the second half of [074](adr-074.md)'s survey. The question is asked by the walk that already exists (`contracts::order::names_in_block`, used for the ordering), and its **over-approximation is what makes it answerable**: a false *mentioned* keeps yesterday's binding and yesterday's warning, a false *not mentioned* would not compile — the two failure modes are not symmetric, so a walk that only has to lean does the work a precise analysis would have to be right for. Not an `allow`, for [074](adr-074.md) D3's reason | Accepted | **yes** - one `match` in the `TryCatch` arm, one public question, one place that spells the name, and six programs |
| [054](adr-054.md) | **`as` names a type Part I 2.2 offers**, and a `std` parameter the language below counts in `usize` is written as the `i64` a program can hold. The parameter direction ADR-048 §4 left open, and the escape hatch it was mistaken for: a cast named any Rust type at all, so `-3 as usize` was 18,446,744,073,709,551,613 in a language where a conversion that does not fit aborts | Accepted | **yes** — `NK1122` at a cast that leaves the eight types; `nikaia_std::count` with a message of its own, because a negative count is not an access out of bounds; the ledger names `usize` in no signature |
| [053](adr-053.md) | **One Rust crate per Nikaia package**, so a package can depend on a package. The manifest key is a crate's local name; rule 2 becomes structural rather than checked, and ADR-046 D4 does not have to widen. Rule 5 — a Nikaia dependency gets the overflow checks — stops being free and is generated per crate. The shape was built by hand and run before it was decided | Accepted | **yes** — a build emits a workspace, always: `members_of` walks the graph, `cargo_workspace` writes a virtual root and one member per package, and rule 2 is enforced nowhere because a transitive crate does not resolve. A dependency's files are still read, for the ledger and the checks, and emitted by nobody. The diamond unifies in the checker too: a type's identity is the package's directory rather than the key a consumer reached it through |
| [057](adr-057.md) | **`Locked[T]` is decided per value**, not per build setting. The safe shape is the floor; at one user thread every lock is the cheap one, because nothing can cross and the mutex would charge exactly what that switch exists to save; at several it is the analysis that already decides the reference count. The two shapes must behave alike, so the mutex shape carries an owner check at +2.0 ns. Displaces Part II 12.2's per-setting split | Accepted | **yes** — both shapes in `nikaia_std::lock` with one behaviour, chosen per value off the count, the cheap one always at `no`. The surface is [059](adr-059.md)'s and the name and constructor are [064](adr-064.md)'s, which is also what let the analysis watch the allocation: **the cheap shape triggers at `yes` now**, for a value nothing crosses a thread with, and a value a task takes comes out atomic in both hulls. §4's **+2.0 ns** owner check was not what shipped — it was a second mutex and a hash per door, at 63.5 ns, and is now 17.3 ([`lock-free.md`](../../lock-free.md) §3) |
| [056](adr-056.md) | **Every name the emitter substitutes is put back**, and where putting it back makes the message say the same thing on both sides, that is an **internal error** carrying the backend's own words. The per-name question — name or translation? — is not asked, so a substitution added later needs no entry: it is the collapse that is noticed, not the name. Restores Part III C.1 for `Shared[T]`, which was deliberately left in Rust's words | Accepted | **yes** — `Rc`/`Arc` become `Shared[T]`, nested hulls whole; the collapse is detected by comparing before and after, so a message rustc itself wrote that way stays a message about the program |
| [055](adr-055.md) | The emitted Rust is `async` where a function can pause, and a plain `fn` where [027](adr-027.md)'s `sync` says it cannot — implicit async, which the language was specified with from [005](adr-005.md) on and the lowering never had. A call to a pausing function carries an `.await` the same way a `throws` one carries a `?` ([023](adr-023.md) D8), the executor is ours over the I/O [038](adr-038.md) D3 built, and `user_parallelism` decides only how many threads it has | Accepted | **all five steps of §6, at both settings** — `rt::exec` interleaves two tasks on one thread (Part II 11.2's sentence, unreachable before), `rt::pool` runs them on threads of their own at `yes`, `async`/`.await` come off the ledger, `std`'s entries suspend for real, and `spawn` and [050](adr-050.md) D2's `overlap` lower. Not built: D6's `Send` as a **refusal** of this compiler's rather than `rustc`'s |
| [061](adr-061.md) | **A `Shared` does not go into code nothing describes** — the refusal a lock already had, for the sentence that was already its reason: a Rust library has one signature, and since ADR-037 D7 the count's is chosen per value. **So at one user thread every count is plain**: nothing can cross there and D1 closed the last way out. Narrows D6, whose floor was bought to keep a *verdict* switch-independent — which it still is | Accepted | **yes** — measured: at `no` a public signature's parameter is plain where it was atomic; at `yes` the per-value answer is unchanged. **D1's own refusal was the last piece and was decided rather than built** until `NK2503` went looking for a count to tell from a lock: `Shared` sat in `contracts::send`'s `CHOSEN` row *and* in its `CONTAINERS` row, the container row answered first, and the refusing row was reached by nothing. A bridge type for the foreign case is reserved and not built |
| [063](adr-063.md) | **A constant written only in literals takes the first type that holds it**, so `2000000000 + 2000000000` compiles where `3000000000 + 1` already did — the unit is the expression and not the token. **A name pins the type its own value took**, so `a + a` is arithmetic in that type and is refused in *this* language's words rather than widened, which would mean walking back from a use to a declaration. One fold for both passes, with the **lookup** as the parameter that separates them | Accepted | **yes** — `crate::fold`, the emitter folding at the outermost expression and carrying the answer inward, and a sequence index and a repeat count left alone because the position gives them their type. Every arrangement of a constant now answers from this compiler; none reaches `rustc`'s overflow lint |
| [060](adr-060.md) | **A literal no use constrains takes the first type that holds it** — `i32`, else `i64` — so `let big = 3000000000` compiles, because it looks like a mistake and is not one. Kotlin's rule as a **fallback** rather than as the rule, so backward inference survives and nothing that compiles today stops. And it needs no analysis: a value an `i32` cannot hold has no second answer a use could ask for | Accepted | **yes** — one rule in the emitter (`integer_literal`), with the negation folded beside it so the question is about the value and not the digits |
| [059](adr-059.md) | **`access` reads and `update` writes**, and the one block that changes a value says `mut` — [094](adr-094.md) D3's word, the lock as the caller. Opened because `access`'s block mutated through its parameter with no word for it; closed by making `access` the read door and `update` the one place a change is written, where the word is on the line. Leaves a write across several locks with no door | Accepted | **yes** — all four doors on both shapes; `update`'s `mut` form is [110](adr-110.md)'s and is not built without a `Default` bound or unsafe code, and all four in `std.contracts`. The write across several locks it left open is [065](adr-065.md)'s `update_all` |
| [058](adr-058.md) | A response body may be bytes the program never allocated — a `Bytes` or a mapping — and, where the request names the file, one it never read; which mechanism carries them is `std`'s choice, because what wins depends on whether anything may be kept: `mmap` per request measured **2.5× worse than plainly reading**. `http::File` takes its root like every path-taking function ([108](adr-108.md)), and a name that leaves it is 404 | Accepted | **no** — `std::http` does not exist; what is built is the bench that decided it |
| [042](adr-042.md) | A view keeps the type it is a view of, arguments and all, and a container whose ledger records a `deref` is seen through once — as a rescue after a comparison has already failed | Accepted | **yes** — `view_of` and `fits_through_deref` in `check`, exercised by `fs::Mapped` and now by `Shared[T]`; a `&Vec[i64]` mismatch is this compiler's `NK1102` where it used to be rustc's |
| [030](adr-030.md) | A program is more than one file, and that is name resolution | Accepted | yes |
| [046](adr-046.md) | `use` makes a unit of code reachable and brings **no name** in — no glob, no braced list, no single name; `use x as y` shortens the prefix, a prefix must be introduced, and one name per file | Accepted | **yes** — the qualified form, all three of D2's refusals in this language's words, `use x as y` resolved away rather than emitted (in one place, on the way to every name), "a prefix must be introduced", and "one name per file" counting the alias |
| [047](adr-047.md) | A **package is a directory**: its files share one namespace and need no `use` between them, privacy is per package, and a library is depended on by **path** — with five rules so the rest is not decided by accident | Accepted | **yes, one level deep** — D1's package is one namespace and one crate root; D2's `path` dependency resolves, becomes a `pub mod` at that root, and all five rules are messages rather than silences. `NK1110` fires for the first time, for items **and** fields, the ledger carrying a field's `pub` because the language below cannot. Not built: everything a version would need. A dependency's own dependencies are [ADR-053](adr-053.md), which replaces the `pub mod` with a crate |
| [035](adr-035.md) | `f"…"` interpolates and `"…"` is text — the mark belongs on the construct | Accepted | yes |
| [048](adr-048.md) | A **length is an `i64`** and so is an index, both conversions emitted rather than written; the machine-width type leaves the writable surface and `u8` is named on it | Accepted | **yes** — four `i64` lengths in `std.contracts`, `NUMERIC` is the four writable types, `xs.len() as i64` and `nikaia_std::index::at` emitted, a negative index reporting as an access out of bounds, and the parentheses around a conversion decided in one place |

### The contract ledger and the type checker

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [020](adr-020.md) | The ledger records what the compiler knows, and a library brings its own | Accepted | yes |
| [101](adr-101.md) | **An error that newly reaches a `catch` is named once, and the ledger commit is the acknowledgement.** Every failure is caught or declared; what was missing was that a `catch { … }` takes a callee's *new* failure in silence. When a `throws` set grows, every handler over that callee is named in the build output, `--locked` fails until the ledger is committed, and after the commit the grown set is the baseline. A handler that matches and one that does not are told alike; the syntax of `catch` is unchanged | Accepted | **yes** — and what said it could not be built is what changed underneath: §5 read *nothing; `std.contracts` writes `throws = ["?"]` on every entry*, and [158](adr-158.md) gave `std` its error type, so the file has nine `throws` lines now and **not one** of them is `["?"]` (the first count of this said *exactly one* and had counted a line of the header comment). D1 is `NK2402`, a warning at the **call** inside the handler; D2 held already, since `--locked` compares the ledger byte for byte |
| [100](adr-100.md) | **A consumer reads a dependency's ledger and never derives it**, which is [020](adr-020.md) D5's rule for `std` applied to every package; **the inference graph is the package**, not the file. The ledger's header carries a SHA-256 per source unit, and a consumer believes the entries while the hashes match, derives that package again where they do not, and compares bytes only under `--locked` — so a build with a hundred unchanged dependencies infers none of them and a stale ledger is a derivation rather than a belief. One crate per package ([053](adr-053.md) D1) is why two builds must not derive the same answer twice: `http`'s crate emits `ok` one way, and its consumer has to write the `.await` the same way | Accepted | **D1–D5** — a package's units are inferred as one graph, its ledger is written in its own root by its own build, the build is ordered dependencies-first, the header carries a SHA-256 per unit, a consumer believes that ledger while the hashes match and derives it again where they do not, and `--locked` compares each byte for byte. `open-work.md`'s false `NK1129` closed with it, in both shapes. Not built: D6's translation of a boundary mismatch as a stale ledger |
| [105](adr-105.md) | **The ledger says `Seq[T]` for what is produced step by step, and `Par[T]` where the steps run at once.** The type language had no word for what `keys()`, `chars()`, `io::lines()` and `map` hand back, so nine entries said `-> ?` and every method on their results went unfound — all 35 silent calls in the corpus. `Seq[T]` takes `sync`/`throws` for the step as a function type does; a `Seq` is consumed by walking, so a second walk is refused, while a container keeps its name and is walked by view; `Par[T]` is the one further distinction, because its lambdas must be `sync`. Neither word is in a program's grammar | Accepted | **steps 1, 2 and 3** — the two words parse, bind, fit and answer as a receiver; the entries are written; `NK2702` refuses a second walk, off the signature rather than off a list of names. `io::lines` was the one that mattered: it said `-> Lines`, so a `for` binding had no type and `line.len()` was a method on `?`. **§1's attribution did not survive the measurement** — 38 unanswered calls before, 33 after. Left: D3's `sync` demand, which waits on `par_iter` having an entry to demand it of |
| [106](adr-106.md) | **A bound takes a path, and the ledger records a trait and each `impl` where they were written.** `[H: http::Handler]` parses as `r: http::Request` already does, and `use` stays as [046](adr-046.md) made it — no name comes in, for a trait as for anything else. A `trait` table holds the methods as ordinary `fn` entries; an `impl` table is written in the package that wrote the `impl`, and whether a type implements a trait is the union over every ledger a program reads, because no single ledger can list implementors completely and a list read as complete would make absence an answer. A call through a bound resolves to the implementing type's own entry, since there is no `dyn` | Accepted | **no** — the path is a parse error and the two tables do not exist |
| [024](adr-024.md) | The type checker says only what is written down; `?` is the absence of a claim | Accepted | yes |
| [027](adr-027.md) | `sync` is earned from the body, not typed by a person | Accepted | yes |
| [028](adr-028.md) | The type checker already knew — the inference needs no second walk | Accepted | yes |
| [029](adr-029.md) | A higher-order function does what its lambda does | Accepted | yes |
| [031](adr-031.md) | A signature may name its receiver's type arguments | Accepted | yes |
| [032](adr-032.md) | A hole is code, and every analysis has to see it | Accepted | yes |

### Errors

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [023](adr-023.md) | What `throws` declares, where the error set lives, and what an error carries | Accepted | `throw`, `catch`, the inferred set, the site |
| [025](adr-025.md) | A loop's step can fail, and the enclosing function gains a `throws` for it | Accepted | yes |
| [036](adr-036.md) | A stack trace is asked for; the site is free | Accepted | yes |

### Ordering and compile-time execution

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [026](adr-026.md) | Compile-time I/O — what a build may read, and what may run while it reads | **Open** | no |
| [033](adr-033.md) | Program order is a guarantee only where it is observable: operations with disjoint **touch** sets have no order between them | Accepted, **provisional** (§7–§8) | four increments — a run of any length on `task::both`, `seq` (D7's keyword, still provisional), the resources `file`/`stdin`/`stdout`/`stderr`/`args`, and **D10's completion pair**: two file reads overlap at `user_parallelism = no` too, because the kernel carries them and no thread carries user code; plus `--overlaps`, which now answers per pair, and D8's manifest key; not a method call, not a non-literal argument, not two writes in flight, no socket or lock until something asks |
| [034](adr-034.md) | A handler that can `return` makes the next statement conditional, so it may not be started early | Accepted | yes |
| [043](adr-043.md) | An arithmetic overflow aborts at every build; wrapping and saturating get Rust's names rather than a new operator; a narrowing cast aborts and an out-of-range literal is a compile error; the check is forced through the generated project with an exception for foreign packages | Accepted | **yes** — the overflow aborts at every build, the names exist for `i32` and `i64`, an out-of-range literal is `NK1116`, a narrowing conversion is checked in the emitted code, and `truncating_i32` says what `as` used to do quietly; a **sum** of constants that cannot fit is `NK1116` too, folded where an operand's declaration pins the type, and a division by a constant zero is `NK1118` (D5.5) |

### The runtime

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [038](adr-038.md) | The runtime and the HTTP server are ours: `rustls` bound, `io_uring` for files, readiness for sockets, and a Rust crate may bring its own runtime under two rules | Accepted | **D3, D4, D5** — the runtime starts before `main`, files complete (with the blocking path as a run-time-detected fallback), sockets signal readiness, and `nikaia-runtime.toml` carries the four settings; D7's **first** rule is enforced by [005](adr-005.md) §5's `Send` check, as far as a foreign signature is honest; D1's HTTP server, D2's `rustls`, D6's parser and D7's second rule are not built |

### Build switches

| ADR | Decides | Status | Built |
| :--- | :--- | :--- | :--- |
| [037](adr-037.md) | Two switches: `target` names the machine, `user_parallelism` says whether the **user's** code may run concurrently — and the compiler's own threads are not the user's | Accepted | D1–D8, the emission included: a `Shared` value is lowered to `Rc` or `Arc` from the count D7 infers for it, so that inference has a real input; `wasm32-unknown` is refused rather than mis-emitted; §3's structural `Send` check is [005](adr-005.md) §5, and the switch reaches its severity and never its verdict |
| [062](adr-062.md) | **A target that lets foreign code call in is a target**, not a third value of `user_parallelism`: that switch bounds *your* code and cannot answer for a caller's threads. So an exported entry point and everything it reaches take the safe shape, and the rest of the library keeps [037](adr-037.md) D7's per-value answer — one build, safe at its boundary, rather than two artifacts or the atomic count on everything. `extern "C"` brings back **not** the representation-in-a-signature problem ([061](adr-061.md) D1 — a C signature can name neither shape) but the ownership of threads; Python's GIL was never the guarantee it is remembered as | Accepted | **no** — and nothing can be: `extern "C"` is a parse error and this repository has no notion of a linkable artifact. The checks need no change at all, because [045](adr-045.md) D1 kept every verdict off the switch |

## What supersedes what

Every supersession in this directory is **partial** — a later record displaces
a section or a numbered decision, never a whole ADR. That is why the superseded
records are still cited by live ones and must not be deleted. Each is named in
the superseding record's own header:

| Displaced or amended | By | What moved |
| :--- | :--- | :--- |
| [086](adr-086.md) D4 | [087](adr-087.md) D3 (withdraws) | the exception that kept `??` out of the head for want of a reproduction. [086](adr-086.md) D3's rule stands and is what [087](adr-087.md) applies; what goes is the idea that an inconsistency in where an existing construct may stand should wait for somebody to trip over it |
| [084](adr-084.md) §4, first row | [086](adr-086.md) §3 (corrects) | what the **−25 %** measured. The `break`-less baseline could not write `while i < n && running`, so it nested an `if`; with the head grammar fixed, the flag and the jump cost the same to 165 instructions over two million turns. An A/B is about the construct only if the construct is the only difference |
| [084](adr-084.md) §3 | [085](adr-085.md) (replaces) | the fix for the `while true` warning — a line in the emitted preamble becomes a change to what is emitted. The defect is the same one; §3 named the wrong remedy for it, and the right one was also the prerequisite [093](adr-093.md) had been missing |
| [070](adr-070.md) D2 | [084](adr-084.md) (answers) | its finding that `break` and `continue` are *"not in the grammar, not in the parser, and not among the 29 reserved words"* — they are now, and the loop they leave is the innermost one. [070](adr-070.md) **D1 is untouched**: its case for a `loop` keyword needs a `break` that hands back a **value**, and [084](adr-084.md) D3 refuses one |
| [194](adr-194.md) D2 (the first of its two products) and D3's operator half | [200](adr-200.md) D1 and D2 (narrow) | **the product, not the reasoning.** *There are two servers, and they are different products* stands and is still why an application's author is not overridden; what goes is the standard file server, cut by the owner for schedule and for value. With it go the *operator's* limits — the caps are the program's, which is what D5's second clause already said — and the localhost default, which was fail-closed because a standard server's author is not there to choose. D3's `--trust` listing of a wide bind is untouched: it belongs to [108](adr-108.md)'s shape and not to either product |
| [038](adr-038.md) D3 (the readiness half) | [199](adr-199.md) D1 (narrows) | the mechanism and not the surface: one poller for the process with a registration each, rather than a worker blocked in a poller per wait. What a program writes is unchanged |
| [002](adr-002.md) D1 (a Rust dependency reaches Cargo verbatim) | [197](adr-197.md) D2 (narrows) | a relative `path` is **resolved** on the way through and written absolute; everything else in the table still travels untouched. [053](adr-053.md) D1's member directory is what made the old rule wrong |
| [195](adr-195.md) D4 (`std` gains a walk and a subprocess) | [196](adr-196.md) D4 (narrows) | both are still wanted and neither is on the reading half's critical path: the Rust half keeps the I/O and hands the grammar the text. The split is a **staging**, and D4 names what a stalled one looks like |
| [193](adr-193.md) D4 (the `syn` step) | [195](adr-195.md) D3 (narrows) | *a real parser (`syn`) replaces the scraper* — the parser is a **grammar**, because the command is a Nikaia program. Every other step of that D4 stands |
| [122](adr-122.md) D1 | [192](adr-192.md) D1 (narrows) | *whether the callee runs it or keeps it* — the run case is `impl AsyncFn(A) -> R` now, chosen by the `keeps` column. D2 and D3 of that record stand, and D3's measurement is what settled it |
| [189](adr-189.md) D3, §4 | [190](adr-190.md) D1, D3 (corrects the reason) | *it waits on `open-work.md` §2.42, with everything else that wants a state between Borrowed and Owned*. Measured, three of the four shapes a `?.` has are **Borrowed** and the fourth is `NK2303`'s. The conclusion - the third case lowers as it did and is not refused - stands; what it waits on is one question about `??` |
| [122](adr-122.md) §1, D1 | [187](adr-187.md) D1, D3 (corrects the premise) | *"Rust has no stable `async` closure"*, which is what chose the boxed future. It is false and was false then; the shape stands on D1's coherence half alone, and whether that is worth 8.7× is on [`open-decisions.md`](../../open-decisions.md) |
| [055](adr-055.md) §6 step 3 | [187](adr-187.md) D2 (corrects the premise) | the same sentence, about a task's body. The conclusion stands for a different reason: a body is a **block** and not something called, so a closure around it would only be called once |
| [033](adr-033.md) D10's coda | [187](adr-187.md) D2 (corrects the premise) | the same sentence, about the pool vehicle. The conclusion stands for a different reason: `rayon::join` takes **synchronous** closures and nothing can `await` inside one |
| [041](adr-041.md) D2 | [049](adr-049.md) D1 (completes) | the automatic names were carried as experimental and warned about; they are withdrawn, and the warning and the arity-from-body rule go with them |
| [033](adr-033.md) D1, D7 | [050](adr-050.md) D1, D7 (displaces) | statement overlapping, `seq` and the `ordering` switch are withdrawn; the touch sets stay, to check an `overlap` block's claim rather than to make one |
| [046](adr-046.md) D1 | [047](adr-047.md) D1 (narrows) | which unit `use` names — a package rather than a file. Every rule in 046 stands and ranges over packages instead; the file boundary it was written against stops existing |
| [043](adr-043.md) D7 | [048](adr-048.md) D1 (narrows) | the sources a `truncating_` entry has — the machine-width type leaves the writable surface, so its two entries go and the larger integer and the float remain |
| [030](adr-030.md) | [047](adr-047.md) D1 (narrows) | a file is a module and `use` names one — the files of a directory are one namespace now, and `use` names the directory |
| [039](adr-039.md) §3 | [045](adr-045.md) D1 (answers) | the open half — a lock could not reach a task, so Part II 12.2's counter was unwritable. The verdict takes the destination; §3's other way out, the `Mutex` floor, is not taken |
| [037](adr-037.md) D6 | [045](adr-045.md) D3 (narrows) | its coda that after D6 "no type answers `may not`" — the lock answers it at a foreign call, so `NK2501` and `NK2502` stop sharing one verdict |
| [022](adr-022.md) D1 | [041](adr-041.md) D1 (amends) | which of the two spellings is recommended — one form with two spellings is unchanged, and both still parse everywhere; the named one is what the specification teaches |
| [005](adr-005.md) §3 | [040](adr-040.md) D1 (narrows) | the ban on a clone the user did not write — it reaches a clone of **data** and no longer a **handle** on a shared value, which copies no data and produces no second value |
| [021](adr-021.md) D5 | [039](adr-039.md) D8 (amends) | the cache-key enumeration: the re-entrancy switch is a fourth dimension, because a build with the check and one without lower the same source to different Rust |
| [005](adr-005.md) D6 | [039](adr-039.md) D2 (narrows) | the runtime reentrancy check as the backstop for re-entering one lock through a chain of `sync` calls — that case is refused at compile time now, and the check becomes self-control of the new rule |
| [002](adr-002.md) D3 | [021](adr-021.md) D9 | Cranelift for "sub-second iterations" — never measured; measured, it buys 25 s against 26 s |
| [013](adr-013.md) D5 | [022](adr-022.md) | `fn:` recorded as a chaining limitation; removed as a second way to say one thing |
| [014](adr-014.md) D3 | [015](adr-015.md) | the measurement, once the backend's eager diagnostics were found to dominate it |
| [014](adr-014.md) D1 | [002](adr-002.md) D4 | *when* `std`'s Nikaia half is lowered — a build script made the compiler a build-dependency of `std`, so Cargo built it again inside every project; release time now, and the `.rs` is committed |
| [023](adr-023.md) D7 | [036](adr-036.md) | `{error:full}` as a format spec; `f"{error.full()}"` needs no new syntax |
| [032](adr-032.md) D5 | [035](adr-035.md) | a brace deciding a literal's type — the `f` decides it now |
| [033](adr-033.md) D5 | [034](adr-034.md) D2 (amends) | "started early" widened from *a branch* to *anything the program might not have performed* |
| [033](adr-033.md) §8.2b | [033](adr-033.md) D10 (narrows) | `effects` degrading to `strict` at `user_parallelism = no` — the category distinction stands, the degradation belongs to `task::both`: two reads the kernel performs carry no user code ([038](adr-038.md) D3) |
| [027](adr-027.md) §7 | [029](adr-029.md) | effect polymorphism — a higher-order function's `sync` now depends on its lambda |
| [028](adr-028.md) D6 | [029](adr-029.md) | a higher-order method carrying no `sync` at all |
| [029](adr-029.md) §4 | [031](adr-031.md) | a signature naming its receiver's type arguments |

## Reserved numbers

A number is **taken when it is claimed here**, not when the record lands. Four
collisions in one afternoon are the reason this section exists: two branches
each took what `ls` said was free, and the later one had to renumber its record,
its citations and its index row every time the other merged first.

| ADR | Claimed for | Where |
| :--- | :--- | :--- |

A row here is a claim and nothing else: it says the number is spoken for, not
what the decision is. Delete the row in the same commit that adds the record.
A claim whose branch is abandoned is deleted by whoever notices.

**When a record does have to move, its citations move with it** — and that half
is the one that gets forgotten, because nothing catches it. A renumbered record
keeps a working link from every sentence that cited its old number, so
`check-adr-refs.py` stays quiet while the sentence now points at whatever record
took the number. Two such sentences were found in `open-work.md` by reading them:
both said *"ADR-073 D4"* about a `break`, and ADR-073's D4 is about whether a
type may be written.

**A guard for this was measured and not built.** The one mechanical shape
available — *a sentence naming an `NK` code and an ADR should name the ADR that
carries the code* — was tried across the docs: 55 sentences name both, 11 have no
overlap, and most of those eleven are correct, because a sentence may name a code
and cite the record that decided the **rule** rather than the one that introduced
the spelling. A gate that is a fifth false alarm teaches people to ignore it. So
this stays a thing to do by hand, and it is written here rather than discovered
again.

## Writing a new one

Take the next free number — free means neither a file above nor a row under
**Reserved numbers**, and the first thing to do with it is to claim it there. Head it with the block every record from
[007](adr-007.md) on uses, in this order:

```markdown
# ADR-0NN: A title that states the decision, not the topic

**Status:** Accepted | Open | Superseded by [ADR-0MM](adr-0MM.md)
**Date:** September 11, 2026
**Target Version:** Nikaia 0.0.8
**Supersedes:** — what exactly, if anything
**Related:** [ADR-0XX](adr-0XX.md) (why it matters here)
```

Then: the context, the decisions as numbered `D1…Dn` so they can be cited, the
consequences, and what is **not** built. A decision nobody can cite by number is
a decision that gets re-litigated.
